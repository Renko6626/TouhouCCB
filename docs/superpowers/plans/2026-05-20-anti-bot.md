# Anti-bot 系统 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实施 spec `docs/superpowers/specs/2026-05-20-anti-bot-design.md` 的 anti-bot 三层防护（L1 ToS + L2 HMAC + L3 白名单 + L4 行为监控）。

**Architecture:** L2 通过 `verify_anti_bot` FastAPI Depends 加到 `/market/{buy,sell,quote}`，依赖 site_config `activity_mode_enabled` 总开关；L3 csv 白名单跳过 L2 + L4；L4 用 APScheduler 每 30 min 扫近 2h Transaction 表，4 个信号触发写 `bot_suspicion` 表；SQLAdmin auto-CRUD admin UI。

**Tech Stack:** FastAPI 0.x + SQLModel + Alembic + APScheduler + pydantic v2 + Vue 3 + Vite + SubtleCrypto

**Branch**: `feat/anti-bot`（已开，已含 spec commit）

---

## File Structure

### Backend 新建

| 文件 | 责任 |
|---|---|
| `backend/app/services/anti_bot.py` | L2 HMAC 验证 + 白名单 csv 解析（纯函数模块） |
| `backend/app/services/bot_detection.py` | L4 信号算法 + scheduler lifecycle |
| `backend/alembic/versions/<TS>_add_bot_suspicion.py` | bot_suspicion 表迁移 |
| `backend/tests/test_anti_bot.py` | L2 unit |
| `backend/tests/test_market_anti_bot_integration.py` | L2 端到端 |
| `backend/tests/test_bot_detection.py` | L4 单测 |
| `backend/tests/test_admin_bot_suspicion.py` | SQLAdmin ModelView 渲染 |

### Backend 修改

| 文件 | 改动 |
|---|---|
| `backend/app/models/base.py` | 加 BotSuspicion model |
| `backend/app/services/loan_migrate.py` | DEFAULT_CONFIGS 加 11 个 keys |
| `backend/app/api/v1/site_config.py` | _WHITELIST 加 11 个 keys |
| `backend/app/api/v1/market.py` | buy/sell/quote 加 Depends(verify_anti_bot) |
| `backend/app/core/config.py` | Settings 加 CLIENT_TOKEN_SECRET |
| `backend/app/main.py` | lifespan 启动 bot_detection scheduler |
| `backend/app/core/admin.py` | 加 BotSuspicionAdmin ModelView |
| `backend/tests/conftest.py` | _disable_scheduler patch bot_detection |

### Frontend 新建

| 文件 | 责任 |
|---|---|
| `thccb-frontend/src/utils/clientToken.ts` | SubtleCrypto HMAC token 生成 |

### Frontend 修改

| 文件 | 改动 |
|---|---|
| `thccb-frontend/src/api/index.ts` | axios interceptor 注入 X-Client-Token / X-Client-TS |

### Config / Docs

| 文件 | 改动 |
|---|---|
| `.env.example` | 加 CLIENT_TOKEN_SECRET + VITE_CLIENT_TOKEN_SECRET 注释 |
| `.github/workflows/ci.yml` | 前端 build job 加 VITE_CLIENT_TOKEN_SECRET env 注入 |
| `docs/tos.md` | 加禁脚本条款 |

---

## Task 1: BotSuspicion model

**Files:**
- Modify: `backend/app/models/base.py`
- Test: `backend/tests/test_bot_suspicion_schema.py` (新)

- [ ] **Step 1: 写失败测**

`backend/tests/test_bot_suspicion_schema.py`:
```python
import sys, os
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

from datetime import datetime, timezone
import pytest
from app.core.database import async_session_maker
from app.models.base import BotSuspicion, User


@pytest.mark.asyncio
async def test_bot_suspicion_can_be_persisted(client):
    async with async_session_maker() as s:
        async with s.begin():
            u = User(username="t", casdoor_id="t_cas")
            s.add(u)
            await s.flush()
            ev = BotSuspicion(
                user_id=u.id,
                signals="high_freq",
                metrics='{"total_trades": 145}',
                window_start=datetime.now(timezone.utc),
                window_end=datetime.now(timezone.utc),
            )
            s.add(ev)
            await s.flush()
            assert ev.id is not None
            assert ev.review_status == "pending"
            assert ev.reviewed_by is None
```

- [ ] **Step 2: 跑确认失败**

`pytest backend/tests/test_bot_suspicion_schema.py -v`
Expected: `ImportError: cannot import name 'BotSuspicion'`

- [ ] **Step 3: 在 `backend/app/models/base.py` 加 model**

末尾加：
```python
class BotSuspicion(SQLModel, table=True):
    __tablename__ = "bot_suspicion"

    id: Optional[int] = Field(default=None, primary_key=True)
    user_id: int = Field(foreign_key="user.id", index=True)
    triggered_at: datetime = Field(
        default_factory=lambda: datetime.now(timezone.utc),
        sa_type=DateTime(timezone=True),
        index=True,
    )
    signals: str = Field(max_length=200)
    metrics: str = Field(max_length=500, default="{}")
    window_start: datetime = Field(sa_type=DateTime(timezone=True))
    window_end: datetime = Field(sa_type=DateTime(timezone=True))
    review_status: str = Field(default="pending", max_length=20)
    reviewed_by: Optional[int] = Field(default=None, foreign_key="user.id")
    reviewed_at: Optional[datetime] = Field(default=None, sa_type=DateTime(timezone=True))
    review_note: Optional[str] = Field(default=None, max_length=500)
```

确保 `datetime` `timezone` `DateTime` `Optional` 已 import（看文件顶部，需要哪个 import 哪个）。

- [ ] **Step 4: 跑确认通过**

`pytest backend/tests/test_bot_suspicion_schema.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/models/base.py backend/tests/test_bot_suspicion_schema.py
git commit -m "feat(model): BotSuspicion 表 schema

字段：user_id, triggered_at, signals (csv), metrics (json str),
window_start/end, review_status (pending/confirmed_bot/false_positive/whitelisted),
reviewed_by/at/note。索引 user_id + triggered_at。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: Alembic migration

**Files:**
- Create: `backend/alembic/versions/<TS>_add_bot_suspicion_table.py`

- [ ] **Step 1: 生成 migration**

```bash
cd backend && alembic revision --autogenerate -m "add bot_suspicion table"
```

期望生成 `versions/<TS>_add_bot_suspicion_table.py` 含 `create_table('bot_suspicion', ...)` + indexes.

- [ ] **Step 2: 检查生成文件**

打开生成的 .py 文件，确认：
- `op.create_table('bot_suspicion', ...)` 字段对齐 Task 1 model
- `op.create_index('ix_bot_suspicion_user_id', 'bot_suspicion', ['user_id'])`
- `op.create_index('ix_bot_suspicion_triggered_at', 'bot_suspicion', ['triggered_at'])`
- 没有意外的其他表/列改动（如果 autogenerate 误识别为 diff，手动删那部分）

如果有 noise 删掉，只保留 bot_suspicion 相关。

- [ ] **Step 3: 跑迁移验证**

```bash
cd backend && /data/sunyunbo/www/TouhouCCB/backend/venv/bin/python -m alembic upgrade head
```
Expected: `Running upgrade ... -> <new_revision>, add bot_suspicion table`

- [ ] **Step 4: 跑测确认表已建**

`pytest backend/tests/test_bot_suspicion_schema.py -v`
Expected: PASS（不再因为 table missing 抛错）

- [ ] **Step 5: Commit**

```bash
git add backend/alembic/versions/*_add_bot_suspicion_table.py
git commit -m "feat(migration): bot_suspicion 表

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: 11 个新 site_config keys + 白名单

**Files:**
- Modify: `backend/app/services/loan_migrate.py`
- Modify: `backend/app/api/v1/site_config.py`
- Test: `backend/tests/test_site_config_anti_bot.py` (新)

- [ ] **Step 1: 写失败测**

`backend/tests/test_site_config_anti_bot.py`:
```python
import sys, os
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

import pytest
from app.core.database import async_session_maker
from app.services import site_config


ANTI_BOT_KEYS = [
    "activity_mode_enabled",
    "quant_whitelist_user_ids",
    "bot_detection_enabled",
    "bot_detection_interval_sec",
    "bot_detection_window_sec",
    "bot_freq_threshold",
    "bot_late_night_threshold",
    "bot_interval_stddev_ms_threshold",
    "bot_fast_follow_trigger_cost",
    "bot_fast_follow_latency_ms",
    "bot_fast_follow_count_threshold",
]


@pytest.mark.asyncio
async def test_anti_bot_configs_seeded_with_defaults(client):
    """启动 lifespan 后 11 个 anti-bot config keys 应该都存在 + 默认值正确。"""
    async with async_session_maker() as s:
        for k in ANTI_BOT_KEYS:
            row = await site_config._fetch(s, k)
            assert row is not None, f"missing default config: {k}"

    # 抽几个默认值核对
    async with async_session_maker() as s:
        assert (await site_config.get_bool(s, "activity_mode_enabled")) is False
        assert (await site_config.get_bool(s, "bot_detection_enabled")) is True
        assert (await site_config.get_int(s, "bot_detection_interval_sec")) == 1800
        assert (await site_config.get_int(s, "bot_detection_window_sec")) == 7200
```

- [ ] **Step 2: 跑确认失败**

`pytest backend/tests/test_site_config_anti_bot.py -v`
Expected: FAIL `missing default config: activity_mode_enabled`

- [ ] **Step 3: 修改 `backend/app/services/loan_migrate.py`**

找到 `DEFAULT_CONFIGS = [...]` 列表，末尾追加：

```python
    # ── Anti-bot (spec 2026-05-20) ──
    ("activity_mode_enabled", "false", "bool"),
    ("quant_whitelist_user_ids", "", "string"),
    ("bot_detection_enabled", "true", "bool"),
    ("bot_detection_interval_sec", "1800", "int"),
    ("bot_detection_window_sec", "7200", "int"),
    ("bot_freq_threshold", "120", "int"),
    ("bot_late_night_threshold", "20", "int"),
    ("bot_interval_stddev_ms_threshold", "100", "int"),
    ("bot_fast_follow_trigger_cost", "500.0", "decimal"),
    ("bot_fast_follow_latency_ms", "1000", "int"),
    ("bot_fast_follow_count_threshold", "3", "int"),
```

- [ ] **Step 4: 修改 `backend/app/api/v1/site_config.py`**

找到 `_WHITELIST = {...}` dict，末尾追加（key → type）：

```python
    "activity_mode_enabled": "bool",
    "quant_whitelist_user_ids": "string",
    "bot_detection_enabled": "bool",
    "bot_detection_interval_sec": "int",
    "bot_detection_window_sec": "int",
    "bot_freq_threshold": "int",
    "bot_late_night_threshold": "int",
    "bot_interval_stddev_ms_threshold": "int",
    "bot_fast_follow_trigger_cost": "decimal",
    "bot_fast_follow_latency_ms": "int",
    "bot_fast_follow_count_threshold": "int",
```

- [ ] **Step 5: 跑测确认通过**

`pytest backend/tests/test_site_config_anti_bot.py -v`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add backend/app/services/loan_migrate.py backend/app/api/v1/site_config.py backend/tests/test_site_config_anti_bot.py
git commit -m "feat(site_config): anti-bot 11 个 key + 默认值 + 白名单

11 个新 config:
- activity_mode_enabled (false, L2 开关)
- quant_whitelist_user_ids ('', L3 csv)
- bot_detection_enabled (true, L4 开关)
- bot_detection_interval_sec (1800)
- bot_detection_window_sec (7200)
- bot_freq_threshold (120)
- bot_late_night_threshold (20)
- bot_interval_stddev_ms_threshold (100)
- bot_fast_follow_trigger_cost (500.0)
- bot_fast_follow_latency_ms (1000)
- bot_fast_follow_count_threshold (3)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: Settings: CLIENT_TOKEN_SECRET

**Files:**
- Modify: `backend/app/core/config.py`
- Test: `backend/tests/test_settings_anti_bot.py` (新)

- [ ] **Step 1: 写失败测**

`backend/tests/test_settings_anti_bot.py`:
```python
import sys, os
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

from app.core.config import settings


def test_settings_has_client_token_secret():
    """settings 应该有 CLIENT_TOKEN_SECRET 字段（默认空 string）。"""
    assert hasattr(settings, "CLIENT_TOKEN_SECRET")
    # 测试环境没设 .env，应该是空 string（pydantic default）
    assert isinstance(settings.CLIENT_TOKEN_SECRET, str)
```

- [ ] **Step 2: 跑确认失败**

`pytest backend/tests/test_settings_anti_bot.py -v`
Expected: FAIL `AttributeError: 'Settings' has no attribute 'CLIENT_TOKEN_SECRET'`

- [ ] **Step 3: 修改 `backend/app/core/config.py`**

在 `Settings` class 内（其他字段附近，比如 `SECRET_KEY` 旁边）加：

```python
    # Anti-bot HMAC SECRET（spec 2026-05-20）
    # 生产 .env 设值；activity_mode_enabled=false 时不验也不需要
    CLIENT_TOKEN_SECRET: str = Field(default="")
```

- [ ] **Step 4: 跑测**

`pytest backend/tests/test_settings_anti_bot.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/core/config.py backend/tests/test_settings_anti_bot.py
git commit -m "feat(config): Settings 加 CLIENT_TOKEN_SECRET 字段

默认空 string；prod .env 设值。activity_mode_enabled=false 时不验。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: anti_bot.py 纯函数模块

**Files:**
- Create: `backend/app/services/anti_bot.py`
- Test: `backend/tests/test_anti_bot.py` (新)

- [ ] **Step 1: 写失败测**

`backend/tests/test_anti_bot.py`:
```python
import sys, os
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

import time
from unittest.mock import patch

import pytest

from app.services.anti_bot import (
    verify_client_token, parse_whitelist, generate_client_token_for_test,
)


def _make_token(secret: str, uid: int, ts: int) -> str:
    """复用 generate_client_token_for_test 算 token，跟 verify 对得上。"""
    return generate_client_token_for_test(secret, uid, ts)


def test_verify_client_token_valid():
    with patch("app.services.anti_bot.settings") as m:
        m.CLIENT_TOKEN_SECRET = "test_secret_32_bytes_hex_abcdef"
        ts = int(time.time())
        token = _make_token("test_secret_32_bytes_hex_abcdef", 1, ts)
        assert verify_client_token(token, str(ts), 1) is True


def test_verify_client_token_expired():
    with patch("app.services.anti_bot.settings") as m:
        m.CLIENT_TOKEN_SECRET = "test_secret_32_bytes_hex_abcdef"
        ts = int(time.time()) - 60  # 60s ago，超过 30s 窗口
        token = _make_token("test_secret_32_bytes_hex_abcdef", 1, ts)
        assert verify_client_token(token, str(ts), 1) is False


def test_verify_client_token_tampered():
    with patch("app.services.anti_bot.settings") as m:
        m.CLIENT_TOKEN_SECRET = "test_secret_32_bytes_hex_abcdef"
        ts = int(time.time())
        token = _make_token("test_secret_32_bytes_hex_abcdef", 1, ts)
        # 篡改最后一字符
        tampered = token[:-1] + ("0" if token[-1] != "0" else "1")
        assert verify_client_token(tampered, str(ts), 1) is False


def test_verify_client_token_wrong_uid():
    with patch("app.services.anti_bot.settings") as m:
        m.CLIENT_TOKEN_SECRET = "test_secret_32_bytes_hex_abcdef"
        ts = int(time.time())
        token = _make_token("test_secret_32_bytes_hex_abcdef", 1, ts)
        # token 是给 uid=1 算的，但当 uid=2 验
        assert verify_client_token(token, str(ts), 2) is False


def test_verify_client_token_invalid_ts():
    with patch("app.services.anti_bot.settings") as m:
        m.CLIENT_TOKEN_SECRET = "test_secret_32_bytes_hex_abcdef"
        assert verify_client_token("any", "not_a_number", 1) is False


def test_verify_client_token_empty_secret():
    with patch("app.services.anti_bot.settings") as m:
        m.CLIENT_TOKEN_SECRET = ""
        ts = int(time.time())
        assert verify_client_token("any", str(ts), 1) is False


def test_parse_whitelist_empty():
    assert parse_whitelist("") == set()


def test_parse_whitelist_basic():
    assert parse_whitelist("1,5,12") == {1, 5, 12}


def test_parse_whitelist_with_spaces_and_garbage():
    assert parse_whitelist(" 1 , 5 , abc , 12 ") == {1, 5, 12}


def test_parse_whitelist_only_garbage():
    assert parse_whitelist("abc,xyz,") == set()
```

- [ ] **Step 2: 跑确认失败**

`pytest backend/tests/test_anti_bot.py -v`
Expected: FAIL with `ImportError: cannot import name 'verify_client_token'`

- [ ] **Step 3: 创建 `backend/app/services/anti_bot.py`**

```python
"""Anti-bot L2 HMAC client_token 验证 + L3 白名单 csv 解析。

设计：纯函数模块，无 DB / 无 IO（除 settings）。verify_client_token 用 HMAC-SHA256
+ 30s 时间窗 + hmac.compare_digest 防 timing attack。

详见 docs/superpowers/specs/2026-05-20-anti-bot-design.md。
"""
from __future__ import annotations
import hashlib
import hmac
import time

from app.core.config import settings


def verify_client_token(token: str, ts: str, uid: int) -> bool:
    """验证前端 HMAC token。失败返 False 不抛异常，调用方决定 403。"""
    if not settings.CLIENT_TOKEN_SECRET:
        return False
    try:
        ts_int = int(ts)
    except (ValueError, TypeError):
        return False
    # 时间窗 30s 容差客户端时钟漂移
    if abs(time.time() - ts_int) > 30:
        return False
    expected = hmac.new(
        settings.CLIENT_TOKEN_SECRET.encode(),
        f"{ts}|{uid}".encode(),
        hashlib.sha256,
    ).hexdigest()
    return hmac.compare_digest(expected, token)


def parse_whitelist(csv_str: str) -> set[int]:
    """解析 csv 白名单 '1, 5, abc, 12' → {1, 5, 12}。容错非数字 + 空格。"""
    if not csv_str:
        return set()
    return {
        int(x.strip()) for x in csv_str.split(",")
        if x.strip().isdigit()
    }


def generate_client_token_for_test(secret: str, uid: int, ts: int) -> str:
    """测试辅助。生产前端用 SubtleCrypto；这是相同算法的服务端版本。"""
    return hmac.new(
        secret.encode(),
        f"{ts}|{uid}".encode(),
        hashlib.sha256,
    ).hexdigest()
```

- [ ] **Step 4: 跑测**

`pytest backend/tests/test_anti_bot.py -v`
Expected: 10 PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/anti_bot.py backend/tests/test_anti_bot.py
git commit -m "feat(anti-bot): verify_client_token + parse_whitelist 核心模块

HMAC-SHA256 + 30s 时间窗 + compare_digest 防 timing attack。
parse_whitelist 容错非数字 + 空格。
generate_client_token_for_test 给单测用。
10 unit tests 全过。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 6: verify_anti_bot Depends + market.py 接入

**Files:**
- Modify: `backend/app/api/v1/market.py`
- Test: `backend/tests/test_market_anti_bot_integration.py` (新)

- [ ] **Step 1: 写失败测**

`backend/tests/test_market_anti_bot_integration.py`:
```python
import sys, os
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

import time
import uuid
from decimal import Decimal
from unittest.mock import patch

import pytest

from app.core.database import async_session_maker
from app.core.users import create_access_token
from app.models.base import Market, MarketStatus, Outcome, SiteConfig, User
from app.services.anti_bot import generate_client_token_for_test


TEST_SECRET = "test_secret_32_bytes_hex_abcdef"


async def _seed_user_and_market():
    suffix = uuid.uuid4().hex[:8]
    async with async_session_maker() as s:
        async with s.begin():
            u = User(
                username=f"ab_{suffix}", email=f"{suffix}@t.com",
                casdoor_id=f"ab_cas_{suffix}", cash=Decimal("1000"),
            )
            s.add(u)
            await s.flush()
            m = Market(title=f"ab_m_{suffix}", description="",
                       liquidity_b=100.0, status=MarketStatus.TRADING, tags="")
            s.add(m)
            await s.flush()
            o = Outcome(market_id=m.id, label="A", total_shares=Decimal("0"))
            s.add(o)
            await s.flush()
            return u.id, m.id, o.id


def _auth_header(uid: int) -> dict:
    return {"Authorization": f"Bearer {create_access_token(uid)}"}


def _client_token_headers(uid: int) -> dict:
    ts = int(time.time())
    token = generate_client_token_for_test(TEST_SECRET, uid, ts)
    return {"X-Client-Token": token, "X-Client-TS": str(ts)}


@pytest.mark.asyncio
async def test_buy_passes_when_activity_mode_off(client):
    """默认 activity_mode=false → 不带 X-Client-Token 也能买。"""
    uid, mid, oid = await _seed_user_and_market()
    r = await client.post(
        "/api/v1/market/quote",
        headers=_auth_header(uid),
        json={"outcome_id": oid, "shares": 1.0, "type": "buy"},
    )
    assert r.status_code == 200, r.text


@pytest.mark.asyncio
async def test_buy_403_when_activity_mode_on_no_header(client):
    uid, mid, oid = await _seed_user_and_market()
    async with async_session_maker() as s:
        async with s.begin():
            row = await s.get(SiteConfig, "activity_mode_enabled")
            row.value = "true"
    from app.services.site_config import clear_cache
    clear_cache()

    with patch("app.services.anti_bot.settings") as m:
        m.CLIENT_TOKEN_SECRET = TEST_SECRET
        r = await client.post(
            "/api/v1/market/quote",
            headers=_auth_header(uid),
            json={"outcome_id": oid, "shares": 1.0, "type": "buy"},
        )
    assert r.status_code == 403, r.text
    assert "anti-bot" in r.json()["detail"].lower()


@pytest.mark.asyncio
async def test_buy_passes_when_activity_mode_on_with_valid_token(client):
    uid, mid, oid = await _seed_user_and_market()
    async with async_session_maker() as s:
        async with s.begin():
            row = await s.get(SiteConfig, "activity_mode_enabled")
            row.value = "true"
    from app.services.site_config import clear_cache
    clear_cache()

    with patch("app.services.anti_bot.settings") as m:
        m.CLIENT_TOKEN_SECRET = TEST_SECRET
        headers = _auth_header(uid)
        headers.update(_client_token_headers(uid))
        r = await client.post(
            "/api/v1/market/quote",
            headers=headers,
            json={"outcome_id": oid, "shares": 1.0, "type": "buy"},
        )
    assert r.status_code == 200, r.text


@pytest.mark.asyncio
async def test_buy_passes_when_user_in_whitelist(client):
    """白名单 user 不带 token 也能通过。"""
    uid, mid, oid = await _seed_user_and_market()
    async with async_session_maker() as s:
        async with s.begin():
            row = await s.get(SiteConfig, "activity_mode_enabled")
            row.value = "true"
            row2 = await s.get(SiteConfig, "quant_whitelist_user_ids")
            row2.value = str(uid)
    from app.services.site_config import clear_cache
    clear_cache()

    with patch("app.services.anti_bot.settings") as m:
        m.CLIENT_TOKEN_SECRET = TEST_SECRET
        r = await client.post(
            "/api/v1/market/quote",
            headers=_auth_header(uid),
            json={"outcome_id": oid, "shares": 1.0, "type": "buy"},
        )
    assert r.status_code == 200, r.text
```

- [ ] **Step 2: 跑确认失败**

`pytest backend/tests/test_market_anti_bot_integration.py -v`
Expected: FAIL（activity_mode 开了但端点没装 Depends，所有测都 200 而不是预期的 403）

- [ ] **Step 3: 修改 `backend/app/api/v1/market.py`**

文件顶部 import 加：
```python
from fastapi import Request
from app.services.anti_bot import verify_client_token, parse_whitelist
```

加 Dependency 函数（放在 router 定义附近，其他 dep 函数旁边）：
```python
async def verify_anti_bot(
    request: Request,
    user: User = Depends(current_active_user),
    db: AsyncSession = Depends(get_async_session),
) -> User:
    """L2 anti-bot 验证 + L3 白名单。

    1. activity_mode=false → 通过
    2. user 在白名单 → 通过
    3. 否则验 HMAC client_token，失败 403
    """
    if not await site_config.get_bool(db, "activity_mode_enabled"):
        return user

    try:
        whitelist_csv = await site_config.get_str(db, "quant_whitelist_user_ids")
    except Exception:
        whitelist_csv = ""
    if user.id in parse_whitelist(whitelist_csv):
        return user

    token = request.headers.get("X-Client-Token")
    ts = request.headers.get("X-Client-TS")
    if not token or not ts:
        raise HTTPException(403, detail="anti-bot 验证缺失（活动期间禁止脚本访问）")

    if not verify_client_token(token, ts, user.id):
        raise HTTPException(403, detail="anti-bot 验证失败")
    return user
```

注意：`site_config.get_str` 可能不存在，看现有 `site_config.py` 是否有；若无，加一个：

```python
# in backend/app/services/site_config.py
async def get_str(session: AsyncSession, key: str) -> str:
    return await _get_raw(session, key)
```

把现有 3 个端点（buy / sell / quote）的 `user: User = Depends(current_active_user)` 替换成 `user: User = Depends(verify_anti_bot)`。

- [ ] **Step 4: 跑测**

`pytest backend/tests/test_market_anti_bot_integration.py -v`
Expected: 4 PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/api/v1/market.py backend/app/services/site_config.py backend/tests/test_market_anti_bot_integration.py
git commit -m "feat(market): /quote /buy /sell 加 verify_anti_bot Depends

activity_mode=false → 透传；
user 在 quant_whitelist → 透传；
否则验 X-Client-Token + X-Client-TS，失败 403。
4 端到端 test 全过。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 7: 前端 clientToken.ts

**Files:**
- Create: `thccb-frontend/src/utils/clientToken.ts`

- [ ] **Step 1: 创建 `thccb-frontend/src/utils/clientToken.ts`**

```typescript
/**
 * Anti-bot L2: SubtleCrypto HMAC-SHA256 client_token 生成。
 *
 * 跟后端 app/services/anti_bot.py::verify_client_token 同算法：
 *   token = HMAC_SHA256(SECRET, "{ts}|{uid}")
 *
 * SECRET 通过 vite build-time env 注入 (VITE_CLIENT_TOKEN_SECRET)，
 * 嵌进 bundle。activity_mode=false 时后端不验，前端发了也无害。
 *
 * 详见 docs/superpowers/specs/2026-05-20-anti-bot-design.md
 */
export async function generateClientToken(
  uid: number,
): Promise<{ token: string; ts: number }> {
  const ts = Math.floor(Date.now() / 1000)
  const secret = import.meta.env.VITE_CLIENT_TOKEN_SECRET as string | undefined
  if (!secret) {
    // dev 环境没设 SECRET，返回空 token (后端 activity_mode=false 时不影响)
    return { token: '', ts }
  }

  const key = await crypto.subtle.importKey(
    'raw',
    new TextEncoder().encode(secret),
    { name: 'HMAC', hash: 'SHA-256' },
    false,
    ['sign'],
  )
  const msg = new TextEncoder().encode(`${ts}|${uid}`)
  const sigBuf = await crypto.subtle.sign('HMAC', key, msg)
  const hex = Array.from(new Uint8Array(sigBuf))
    .map((b) => b.toString(16).padStart(2, '0'))
    .join('')
  return { token: hex, ts }
}
```

- [ ] **Step 2: 验证 type-check**

```bash
cd thccb-frontend && /data/sunyunbo/www/TouhouCCB/thccb-frontend/node_modules/.bin/vue-tsc --noEmit
```
Expected: no output (clean)

- [ ] **Step 3: Commit**

```bash
git add thccb-frontend/src/utils/clientToken.ts
git commit -m "feat(frontend): clientToken.ts SubtleCrypto HMAC 生成工具

跟后端 anti_bot.verify_client_token 同算法 (HMAC-SHA256 'ts|uid')。
SECRET 通过 vite env VITE_CLIENT_TOKEN_SECRET 注入 bundle。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 8: 前端 axios interceptor 注入 header

**Files:**
- Modify: `thccb-frontend/src/api/index.ts`

- [ ] **Step 1: 读现有 interceptors**

```bash
grep -n "interceptors\|api.interceptors" thccb-frontend/src/api/index.ts | head -10
```
看现有 request interceptor 怎么写的。

- [ ] **Step 2: 在 request interceptor 加 anti-bot header 注入**

在 `thccb-frontend/src/api/index.ts` 的 request interceptor 内（auth header 之后）加：

```typescript
// Anti-bot L2: /market/{buy,sell,quote} 加 X-Client-Token + X-Client-TS
// activity_mode=false 时后端不验；这里始终发，避免分支
if (config.url?.match(/\/api\/v1\/market\/(buy|sell|quote)/)) {
  // authStore 在这里 import 可能形成循环，用 dynamic import
  const { useAuthStore } = await import('@/stores/auth')
  const authStore = useAuthStore()
  const uid = authStore.user?.id
  if (uid) {
    const { generateClientToken } = await import('@/utils/clientToken')
    const { token, ts } = await generateClientToken(uid)
    if (token) {
      config.headers['X-Client-Token'] = token
      config.headers['X-Client-TS'] = String(ts)
    }
  }
}
```

注意：interceptor 已经是 async function 才能 await。如果不是，把它改成 async。

- [ ] **Step 3: 验证 type-check**

```bash
cd thccb-frontend && /data/sunyunbo/www/TouhouCCB/thccb-frontend/node_modules/.bin/vue-tsc --noEmit
```
Expected: no output

- [ ] **Step 4: Commit**

```bash
git add thccb-frontend/src/api/index.ts
git commit -m "feat(frontend): axios interceptor 自动注入 X-Client-Token

对 /market/{buy,sell,quote} 三个端点自动调 generateClientToken 加 header。
dev SECRET 为空时 token='' 发空 header，后端 activity_mode=false 时不影响。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 9: bot_detection.py 信号算法 (high_freq + late_night + regular_interval)

**Files:**
- Create: `backend/app/services/bot_detection.py`
- Test: `backend/tests/test_bot_detection_signals.py` (新)

- [ ] **Step 1: 写失败测**

`backend/tests/test_bot_detection_signals.py`:
```python
import sys, os
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

from datetime import datetime, timezone, timedelta
import statistics

import pytest

from app.services.bot_detection import (
    compute_high_freq_signal,
    compute_late_night_signal,
    compute_regular_interval_signal,
)


def _txn(ts_offset_sec: float, cost: float = 10.0):
    """轻量"假交易"对象，只有 timestamp 和 cost。"""
    class T:
        timestamp = datetime.now(timezone.utc) - timedelta(seconds=ts_offset_sec)
    t = T()
    t.cost = cost
    return t


def test_high_freq_triggers_when_threshold_reached():
    # 130 笔交易，阈值 120 → 触发
    txns = [_txn(i) for i in range(130)]
    assert compute_high_freq_signal(txns, threshold=120) is True


def test_high_freq_not_triggered_below_threshold():
    txns = [_txn(i) for i in range(50)]
    assert compute_high_freq_signal(txns, threshold=120) is False


def test_late_night_signal():
    """凌晨 03-06 (Asia/Shanghai = UTC+8 → UTC 19-22) 时段的交易 ≥ 阈值 → 触发。"""
    now_utc = datetime.now(timezone.utc)
    # 构造 25 笔 UTC 19:00 的交易（北京时间 03:00）
    late_txns = []
    base = now_utc.replace(hour=19, minute=0, second=0, microsecond=0)
    for i in range(25):
        class T: pass
        t = T()
        t.timestamp = base + timedelta(minutes=i)
        t.cost = 10.0
        late_txns.append(t)
    # 加 10 笔白天的，不计
    day_base = now_utc.replace(hour=10, minute=0, second=0, microsecond=0)
    day_txns = []
    for i in range(10):
        class T: pass
        t = T()
        t.timestamp = day_base + timedelta(minutes=i)
        t.cost = 10.0
        day_txns.append(t)
    all_txns = late_txns + day_txns
    assert compute_late_night_signal(all_txns, threshold=20) is True


def test_late_night_not_triggered_below_threshold():
    now_utc = datetime.now(timezone.utc)
    base = now_utc.replace(hour=19, minute=0, second=0, microsecond=0)
    txns = []
    for i in range(5):
        class T: pass
        t = T()
        t.timestamp = base + timedelta(minutes=i)
        t.cost = 10.0
        txns.append(t)
    assert compute_late_night_signal(txns, threshold=20) is False


def test_regular_interval_triggers_when_stddev_low():
    """间隔几乎相同 (每次 1.0s) → stddev_ms 接近 0 → 触发。"""
    base = datetime.now(timezone.utc)
    txns = []
    for i in range(10):
        class T: pass
        t = T()
        t.timestamp = base + timedelta(seconds=i * 1.0)
        t.cost = 10.0
        txns.append(t)
    assert compute_regular_interval_signal(txns, stddev_ms_threshold=100) is True


def test_regular_interval_not_triggered_when_stddev_high():
    """间隔差异大 → 不触发。"""
    base = datetime.now(timezone.utc)
    intervals = [0.5, 3.7, 1.2, 8.4, 2.1, 6.9, 0.9, 5.5]
    txns = [_txn(0)]
    cumulative = 0.0
    for d in intervals:
        cumulative += d
        class T: pass
        t = T()
        t.timestamp = base + timedelta(seconds=cumulative)
        t.cost = 10.0
        txns.append(t)
    assert compute_regular_interval_signal(txns, stddev_ms_threshold=100) is False


def test_regular_interval_skipped_below_3_txns():
    """少于 3 笔 → 算法跳过，返 False。"""
    txns = [_txn(0), _txn(1)]
    assert compute_regular_interval_signal(txns, stddev_ms_threshold=100) is False
```

- [ ] **Step 2: 跑确认失败**

`pytest backend/tests/test_bot_detection_signals.py -v`
Expected: FAIL `ImportError`

- [ ] **Step 3: 创建 `backend/app/services/bot_detection.py`** （仅 3 个信号函数版本，scheduler 等 Task 11 加）

```python
"""Anti-bot L4: 行为监控信号算法 + scheduler。

详见 docs/superpowers/specs/2026-05-20-anti-bot-design.md。

本文件分两部分：
1. 信号算法（pure functions，本 Task 9 + 10）
2. scheduler + 主循环（Task 11-12）

每 30 min 扫近 2h Transaction，触发任一信号 → 写 bot_suspicion。
白名单 user 跳过扫描；6h 内同信号同 user 不重写。
"""
from __future__ import annotations
import statistics
from datetime import datetime, timezone, timedelta
from typing import Iterable

# UTC+8 凌晨 03-06 = UTC 19-22
LATE_NIGHT_UTC_HOURS = {19, 20, 21}  # 19:00-22:00 UTC = 03:00-06:00 CST


def compute_high_freq_signal(transactions: Iterable, threshold: int) -> bool:
    """高频信号：窗口内 user 交易总笔数 ≥ threshold。"""
    return sum(1 for _ in transactions) >= threshold


def compute_late_night_signal(transactions: Iterable, threshold: int) -> bool:
    """凌晨信号：03-06 CST (= 19-22 UTC) 时段交易笔数 ≥ threshold。"""
    count = sum(
        1 for tx in transactions
        if tx.timestamp.astimezone(timezone.utc).hour in LATE_NIGHT_UTC_HOURS
    )
    return count >= threshold


def compute_regular_interval_signal(
    transactions: list, stddev_ms_threshold: int,
) -> bool:
    """间隔规律性信号：≥ 3 笔交易 + 相邻间隔的 stddev_ms < threshold。

    少于 3 笔不算（样本太小）。
    """
    if len(transactions) < 3:
        return False
    sorted_txns = sorted(transactions, key=lambda t: t.timestamp)
    intervals_ms = []
    for i in range(1, len(sorted_txns)):
        delta = sorted_txns[i].timestamp - sorted_txns[i - 1].timestamp
        intervals_ms.append(delta.total_seconds() * 1000)
    if len(intervals_ms) < 2:
        return False
    sd = statistics.stdev(intervals_ms)
    return sd < stddev_ms_threshold
```

- [ ] **Step 4: 跑测**

`pytest backend/tests/test_bot_detection_signals.py -v`
Expected: 7 PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/bot_detection.py backend/tests/test_bot_detection_signals.py
git commit -m "feat(bot_detection): high_freq + late_night + regular_interval 信号

3 个纯函数信号:
- high_freq: 窗口内总笔数 ≥ threshold
- late_night: UTC+8 03-06 时段笔数 ≥ threshold
- regular_interval: ≥3 笔且间隔 stddev_ms < threshold (少于 3 笔跳过)

7 unit tests 全过。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 10: fast_follow 信号

**Files:**
- Modify: `backend/app/services/bot_detection.py`
- Modify: `backend/tests/test_bot_detection_signals.py`

- [ ] **Step 1: 加失败测**

在 `backend/tests/test_bot_detection_signals.py` 加：

```python
from decimal import Decimal
from app.services.bot_detection import compute_fast_follow_signal


def _txn_with_market(ts_offset_sec: float, *, market_id: int, user_id: int, cost: float = 10.0):
    """带 market_id / user_id 的交易对象，供 fast_follow 测试用。"""
    base = datetime.now(timezone.utc) - timedelta(hours=1)
    class T: pass
    t = T()
    t.timestamp = base + timedelta(seconds=ts_offset_sec)
    t.cost = cost
    t.market_id = market_id
    t.user_id = user_id
    return t


def test_fast_follow_triggers_when_user_follows_big_trades():
    """trigger user 在 t=0/100/200 发 600 块大单；target user 每次后 500ms 内同 market 跟进 → 3 次跟进，阈值 3 → 触发。"""
    trigger_user_txns = [
        _txn_with_market(0, market_id=1, user_id=99, cost=600),
        _txn_with_market(100, market_id=1, user_id=99, cost=600),
        _txn_with_market(200, market_id=1, user_id=99, cost=600),
    ]
    target_user_txns = [
        _txn_with_market(0.5, market_id=1, user_id=1, cost=10),
        _txn_with_market(100.4, market_id=1, user_id=1, cost=10),
        _txn_with_market(200.3, market_id=1, user_id=1, cost=10),
    ]
    all_txns = trigger_user_txns + target_user_txns
    assert compute_fast_follow_signal(
        all_txns, target_user_id=1,
        trigger_cost_threshold=Decimal("500"),
        latency_ms=1000, count_threshold=3,
    ) is True


def test_fast_follow_self_followups_dont_count():
    """user 自己的大单不算 trigger（自跟进不算）。"""
    txns = [
        _txn_with_market(0, market_id=1, user_id=1, cost=600),     # self trigger
        _txn_with_market(0.5, market_id=1, user_id=1, cost=10),    # self followup, 不算
        _txn_with_market(100, market_id=1, user_id=1, cost=600),
        _txn_with_market(100.4, market_id=1, user_id=1, cost=10),
        _txn_with_market(200, market_id=1, user_id=1, cost=600),
        _txn_with_market(200.3, market_id=1, user_id=1, cost=10),
    ]
    assert compute_fast_follow_signal(
        txns, target_user_id=1,
        trigger_cost_threshold=Decimal("500"),
        latency_ms=1000, count_threshold=3,
    ) is False


def test_fast_follow_cross_market_doesnt_count():
    """trigger 在 market_id=1，target user 跟进在 market_id=2 → 不算。"""
    txns = [
        _txn_with_market(0, market_id=1, user_id=99, cost=600),
        _txn_with_market(0.5, market_id=2, user_id=1, cost=10),   # 跨市场跟进，不算
        _txn_with_market(100, market_id=1, user_id=99, cost=600),
        _txn_with_market(100.4, market_id=2, user_id=1, cost=10),
        _txn_with_market(200, market_id=1, user_id=99, cost=600),
        _txn_with_market(200.3, market_id=2, user_id=1, cost=10),
    ]
    assert compute_fast_follow_signal(
        txns, target_user_id=1,
        trigger_cost_threshold=Decimal("500"),
        latency_ms=1000, count_threshold=3,
    ) is False


def test_fast_follow_below_count_threshold():
    """只 2 次跟进，阈值 3 → 不触发。"""
    txns = [
        _txn_with_market(0, market_id=1, user_id=99, cost=600),
        _txn_with_market(0.5, market_id=1, user_id=1, cost=10),
        _txn_with_market(100, market_id=1, user_id=99, cost=600),
        _txn_with_market(100.4, market_id=1, user_id=1, cost=10),
    ]
    assert compute_fast_follow_signal(
        txns, target_user_id=1,
        trigger_cost_threshold=Decimal("500"),
        latency_ms=1000, count_threshold=3,
    ) is False
```

- [ ] **Step 2: 跑确认失败**

`pytest backend/tests/test_bot_detection_signals.py::test_fast_follow_triggers_when_user_follows_big_trades -v`
Expected: FAIL `ImportError: cannot import name 'compute_fast_follow_signal'`

- [ ] **Step 3: 在 `backend/app/services/bot_detection.py` 加 fast_follow**

文件顶部 import 加：
```python
from decimal import Decimal
```

末尾加：
```python
def compute_fast_follow_signal(
    all_transactions: list,
    *,
    target_user_id: int,
    trigger_cost_threshold: Decimal,
    latency_ms: int,
    count_threshold: int,
) -> bool:
    """fast_follow 信号: target user 在别人大单后 latency 内同 market 跟进 ≥ count 次。

    - trigger: any user (除 target user 自己) 的交易 cost ≥ trigger_cost_threshold
    - 跟进: target user 在 trigger 后 latency_ms 内、同 market_id 的交易
    - 阈值: 跟进次数 ≥ count_threshold

    详见 spec 数据流 § L4 信号算法 fast_follow。
    """
    # 提取 triggers（排除自己的）
    triggers = [
        tx for tx in all_transactions
        if abs(float(tx.cost)) >= float(trigger_cost_threshold)
        and tx.user_id != target_user_id
    ]
    if not triggers:
        return False

    # target user 在各 market 上的交易，按 (market_id, timestamp) 排序
    target_txns = sorted(
        (tx for tx in all_transactions if tx.user_id == target_user_id),
        key=lambda t: (t.market_id, t.timestamp),
    )

    follow_count = 0
    for trigger in triggers:
        deadline = trigger.timestamp + timedelta(milliseconds=latency_ms)
        # 找 target user 在 trigger 之后 latency 内同 market 的最近一笔
        for tt in target_txns:
            if (
                tt.market_id == trigger.market_id
                and trigger.timestamp < tt.timestamp <= deadline
            ):
                follow_count += 1
                break  # 一个 trigger 只算一次跟进

    return follow_count >= count_threshold
```

- [ ] **Step 4: 跑全部信号测**

`pytest backend/tests/test_bot_detection_signals.py -v`
Expected: 11 PASS (7 老 + 4 新)

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/bot_detection.py backend/tests/test_bot_detection_signals.py
git commit -m "feat(bot_detection): fast_follow 信号 (毫秒级跟进 bot 特征)

fast_follow: target user 在别人大单 (cost ≥ threshold) 后 latency_ms 内
同 market_id 跟进 ≥ count 次 → 触发。

排除自跟进 (trigger user != target user)。
排除跨 market (跟进必须同 market_id)。
一个 trigger 只算一次跟进 (防止短时间内多次跟进重复计数)。

4 新 unit tests:
- 基本触发
- 自跟进排除
- 跨 market 排除  
- 低于 count_threshold

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 11: bot_detection 主循环 + dedup + 白名单

**Files:**
- Modify: `backend/app/services/bot_detection.py`
- Test: `backend/tests/test_bot_detection_main_loop.py` (新)

- [ ] **Step 1: 写失败测**

`backend/tests/test_bot_detection_main_loop.py`:
```python
import sys, os
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

import uuid
from datetime import datetime, timezone, timedelta
from decimal import Decimal

import pytest
from sqlalchemy import select

from app.core.database import async_session_maker
from app.models.base import (
    BotSuspicion, Market, MarketStatus, Outcome, SiteConfig,
    Transaction, TransactionType, User,
)
from app.services import bot_detection


async def _enable_detection_with_low_threshold():
    """改 site_config 让阈值低，方便测触发。"""
    async with async_session_maker() as s:
        async with s.begin():
            for k, v in [
                ("bot_detection_enabled", "true"),
                ("bot_detection_window_sec", "7200"),
                ("bot_freq_threshold", "5"),  # 低到 5 笔就触发
                ("bot_late_night_threshold", "999"),
                ("bot_interval_stddev_ms_threshold", "1"),
                ("bot_fast_follow_trigger_cost", "999999.0"),
                ("bot_fast_follow_latency_ms", "1000"),
                ("bot_fast_follow_count_threshold", "999"),
            ]:
                row = await s.get(SiteConfig, k)
                row.value = v
    from app.services.site_config import clear_cache
    clear_cache()


async def _seed_user_with_n_recent_txns(n: int):
    suffix = uuid.uuid4().hex[:6]
    async with async_session_maker() as s:
        async with s.begin():
            u = User(username=f"bd_{suffix}", casdoor_id=f"bd_cas_{suffix}",
                     cash=Decimal("1000"))
            s.add(u)
            await s.flush()
            m = Market(title=f"bd_m_{suffix}", description="",
                       liquidity_b=100.0, status=MarketStatus.TRADING, tags="")
            s.add(m)
            await s.flush()
            o = Outcome(market_id=m.id, label="A", total_shares=Decimal("0"))
            s.add(o)
            await s.flush()
            now = datetime.now(timezone.utc)
            for i in range(n):
                tx = Transaction(
                    user_id=u.id, outcome_id=o.id,
                    type=TransactionType.BUY, shares=Decimal("1"),
                    cost=Decimal("10"), price=Decimal("0.5"),
                    gross=Decimal("10"), fee=Decimal("0"),
                    timestamp=now - timedelta(minutes=i),
                )
                s.add(tx)
            return u.id


@pytest.mark.asyncio
async def test_main_loop_triggers_signal_writes_bot_suspicion(client):
    await _enable_detection_with_low_threshold()
    uid = await _seed_user_with_n_recent_txns(10)

    result = await bot_detection.run_bot_detection_once()
    assert result["enabled"] is True
    assert result["scanned_users"] >= 1
    assert result["triggered_count"] >= 1

    async with async_session_maker() as s:
        rows = (await s.execute(
            select(BotSuspicion).where(BotSuspicion.user_id == uid)
        )).scalars().all()
        assert len(rows) == 1
        assert "high_freq" in rows[0].signals
        assert rows[0].review_status == "pending"


@pytest.mark.asyncio
async def test_main_loop_skips_whitelist_user(client):
    await _enable_detection_with_low_threshold()
    uid = await _seed_user_with_n_recent_txns(10)
    # 加白名单
    async with async_session_maker() as s:
        async with s.begin():
            row = await s.get(SiteConfig, "quant_whitelist_user_ids")
            row.value = str(uid)
    from app.services.site_config import clear_cache
    clear_cache()

    result = await bot_detection.run_bot_detection_once()

    async with async_session_maker() as s:
        rows = (await s.execute(
            select(BotSuspicion).where(BotSuspicion.user_id == uid)
        )).scalars().all()
        assert len(rows) == 0, "whitelist user 不应写 BotSuspicion"


@pytest.mark.asyncio
async def test_main_loop_dedup_within_6h(client):
    """6h 内同 user 同信号已存在 pending → 不重写。"""
    await _enable_detection_with_low_threshold()
    uid = await _seed_user_with_n_recent_txns(10)

    # 第一次跑：写一条
    await bot_detection.run_bot_detection_once()
    async with async_session_maker() as s:
        rows = (await s.execute(
            select(BotSuspicion).where(BotSuspicion.user_id == uid)
        )).scalars().all()
        assert len(rows) == 1

    # 第二次立刻跑：6h dedup 应跳过
    await bot_detection.run_bot_detection_once()
    async with async_session_maker() as s:
        rows = (await s.execute(
            select(BotSuspicion).where(BotSuspicion.user_id == uid)
        )).scalars().all()
        assert len(rows) == 1, "6h dedup 失败，重复写了"


@pytest.mark.asyncio
async def test_main_loop_disabled_returns_skip_with_full_fields(client):
    """detection_enabled=false → 早返回但字段集完整 (review I-1 教训)。"""
    async with async_session_maker() as s:
        async with s.begin():
            row = await s.get(SiteConfig, "bot_detection_enabled")
            row.value = "false"
    from app.services.site_config import clear_cache
    clear_cache()

    result = await bot_detection.run_bot_detection_once()
    expected_keys = {"enabled", "scanned_users", "triggered_count",
                     "skipped_count", "duration_ms"}
    assert set(result.keys()) >= expected_keys
    assert result["enabled"] is False
```

- [ ] **Step 2: 跑确认失败**

`pytest backend/tests/test_bot_detection_main_loop.py -v`
Expected: FAIL `AttributeError: module 'bot_detection' has no attribute 'run_bot_detection_once'`

- [ ] **Step 3: 在 `backend/app/services/bot_detection.py` 加主循环**

文件顶部 import 加：
```python
import json
import logging
import time
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.database import async_session_maker
from app.models.base import BotSuspicion, Transaction, User
from app.services import site_config
from app.services.anti_bot import parse_whitelist

_logger = logging.getLogger("thccb.bot_detection")
_DEDUP_WINDOW_SEC = 6 * 3600  # 6 小时 dedup
```

末尾加：

```python
def _empty_result(enabled: bool, duration_ms: int = 0) -> dict:
    return {
        "enabled": enabled,
        "scanned_users": 0,
        "triggered_count": 0,
        "skipped_count": 0,
        "duration_ms": duration_ms,
    }


async def _recent_signal_exists(
    db: AsyncSession, user_id: int, signal: str, now: datetime,
) -> bool:
    """6h 内是否已有 pending BotSuspicion 含此 signal。"""
    threshold = now - timedelta(seconds=_DEDUP_WINDOW_SEC)
    stmt = (
        select(BotSuspicion.id)
        .where(
            BotSuspicion.user_id == user_id,
            BotSuspicion.review_status == "pending",
            BotSuspicion.triggered_at > threshold,
            BotSuspicion.signals.contains(signal),
        )
        .limit(1)
    )
    return (await db.execute(stmt)).scalar() is not None


async def run_bot_detection_once() -> dict:
    """每 30 min 一次：扫近 window_sec 的 Transaction，触发任一信号 → 写 BotSuspicion。

    白名单 user 跳过；同 user 同 signal 6h 内已 pending 不重写。
    """
    start_ts = time.monotonic()
    now = datetime.now(timezone.utc)

    async with async_session_maker() as db:
        try:
            cfg = await site_config.get_many(db, [
                "bot_detection_enabled", "bot_detection_window_sec",
                "bot_freq_threshold", "bot_late_night_threshold",
                "bot_interval_stddev_ms_threshold",
                "bot_fast_follow_trigger_cost",
                "bot_fast_follow_latency_ms",
                "bot_fast_follow_count_threshold",
                "quant_whitelist_user_ids",
            ])
        except Exception:
            _logger.exception("bot_detection_config_load_failed")
            return _empty_result(False, int((time.monotonic() - start_ts) * 1000))

        enabled = cfg.get("bot_detection_enabled", "false").lower() in ("true", "1", "yes")
        if not enabled:
            return _empty_result(False, int((time.monotonic() - start_ts) * 1000))

        try:
            window_sec = int(cfg["bot_detection_window_sec"])
            freq_thr = int(cfg["bot_freq_threshold"])
            late_thr = int(cfg["bot_late_night_threshold"])
            stddev_thr = int(cfg["bot_interval_stddev_ms_threshold"])
            ff_cost = Decimal(cfg["bot_fast_follow_trigger_cost"])
            ff_lat = int(cfg["bot_fast_follow_latency_ms"])
            ff_count = int(cfg["bot_fast_follow_count_threshold"])
        except (KeyError, ValueError, Exception):
            _logger.exception("bot_detection_config_parse_failed")
            return _empty_result(True, int((time.monotonic() - start_ts) * 1000))

        whitelist = parse_whitelist(cfg.get("quant_whitelist_user_ids", ""))

        window_start = now - timedelta(seconds=window_sec)
        all_txns_stmt = (
            select(Transaction)
            .where(Transaction.timestamp >= window_start)
            .order_by(Transaction.timestamp.asc())
        )
        all_txns = (await db.execute(all_txns_stmt)).scalars().all()
        # 给每条 txn 补 market_id（fast_follow 要用；通过 outcome 关联）
        from app.models.base import Outcome
        outcome_ids = {tx.outcome_id for tx in all_txns}
        if outcome_ids:
            outcomes = (await db.execute(
                select(Outcome.id, Outcome.market_id)
                .where(Outcome.id.in_(outcome_ids))
            )).all()
            o_to_m = {o.id: o.market_id for o in outcomes}
            for tx in all_txns:
                tx.market_id = o_to_m.get(tx.outcome_id)

        # 按 user_id 分组
        txns_by_user: dict[int, list] = {}
        for tx in all_txns:
            txns_by_user.setdefault(tx.user_id, []).append(tx)

        scanned_users = 0
        triggered_count = 0
        skipped_count = 0

        for uid, user_txns in txns_by_user.items():
            if uid in whitelist:
                skipped_count += 1
                continue
            scanned_users += 1

            triggered_signals: list[str] = []
            metrics: dict = {"total_trades": len(user_txns)}

            if compute_high_freq_signal(user_txns, freq_thr):
                triggered_signals.append("high_freq")
            if compute_late_night_signal(user_txns, late_thr):
                triggered_signals.append("late_night")
                metrics["late_night_trades"] = sum(
                    1 for tx in user_txns
                    if tx.timestamp.astimezone(timezone.utc).hour in LATE_NIGHT_UTC_HOURS
                )
            if compute_regular_interval_signal(user_txns, stddev_thr):
                triggered_signals.append("regular_interval")
            if compute_fast_follow_signal(
                all_txns, target_user_id=uid,
                trigger_cost_threshold=ff_cost,
                latency_ms=ff_lat, count_threshold=ff_count,
            ):
                triggered_signals.append("fast_follow")

            if not triggered_signals:
                continue

            # 检查 dedup
            new_signals = []
            for sig in triggered_signals:
                if not await _recent_signal_exists(db, uid, sig, now):
                    new_signals.append(sig)
            if not new_signals:
                continue

            ev = BotSuspicion(
                user_id=uid,
                triggered_at=now,
                signals=",".join(new_signals),
                metrics=json.dumps(metrics),
                window_start=window_start,
                window_end=now,
            )
            db.add(ev)
            await db.flush()
            triggered_count += 1
            _logger.warning(
                "bot_suspicion_recorded",
                extra={"user_id": uid, "signals": ",".join(new_signals)},
            )

        await db.commit()

    duration_ms = int((time.monotonic() - start_ts) * 1000)
    result = {
        "enabled": True,
        "scanned_users": scanned_users,
        "triggered_count": triggered_count,
        "skipped_count": skipped_count,
        "duration_ms": duration_ms,
    }
    _logger.info("bot_detection_done", extra=result)
    return result
```

- [ ] **Step 4: 跑测**

`pytest backend/tests/test_bot_detection_main_loop.py -v`
Expected: 4 PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/bot_detection.py backend/tests/test_bot_detection_main_loop.py
git commit -m "feat(bot_detection): run_bot_detection_once 主循环 + 白名单跳过 + 6h dedup

- site_config 批量读 11 个 key
- enabled=false → 早返回完整字段集 schema (review I-1 教训)
- 扫近 window_sec Transaction，按 user 分组
- whitelist user 跳过 (skipped_count 计入)
- 对每 user 算 4 信号；任一触发查 6h dedup → 无则写 BotSuspicion
- result dict 含 enabled/scanned_users/triggered_count/skipped_count/duration_ms

4 集成 test 全过。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 12: bot_detection scheduler + main.py 接入 + conftest

**Files:**
- Modify: `backend/app/services/bot_detection.py`
- Modify: `backend/app/main.py`
- Modify: `backend/tests/conftest.py`

- [ ] **Step 1: 在 `backend/app/services/bot_detection.py` 加 scheduler lifecycle**

文件顶部 import 加：
```python
from typing import Optional
from apscheduler.schedulers.asyncio import AsyncIOScheduler
```

末尾加：
```python
_scheduler: Optional[AsyncIOScheduler] = None
_JOB_ID = "bot_detection_tick"


async def _tick_safe():
    try:
        await run_bot_detection_once()
    except Exception:
        _logger.exception("bot_detection_tick_failed")


async def start_scheduler() -> None:
    """FastAPI lifespan 调。从 site_config 读 interval 启动 scheduler。"""
    global _scheduler
    if _scheduler is not None:
        return
    async with async_session_maker() as db:
        try:
            interval = await site_config.get_int(db, "bot_detection_interval_sec")
        except Exception:
            interval = 1800
    interval = max(60, min(7200, interval))
    _scheduler = AsyncIOScheduler(timezone="UTC")
    _scheduler.add_job(
        _tick_safe, "interval", seconds=interval,
        id=_JOB_ID, max_instances=1,
    )
    _scheduler.start()
    _logger.info("bot_detection_scheduler_started", extra={"interval_sec": interval})


async def stop_scheduler() -> None:
    global _scheduler
    if _scheduler is not None:
        _scheduler.shutdown(wait=False)
        _scheduler = None


async def reschedule(interval_sec: int) -> None:
    """管理员改 site_config 后调，热调度。"""
    global _scheduler
    if _scheduler is None:
        return
    interval = max(60, min(7200, interval_sec))
    _scheduler.reschedule_job(_JOB_ID, trigger="interval", seconds=interval)
    _logger.info("bot_detection_scheduler_rescheduled", extra={"interval_sec": interval})
```

- [ ] **Step 2: 修改 `backend/app/main.py`**

import 段加：
```python
from app.services.bot_detection import (
    start_scheduler as start_bot_detection_scheduler,
    stop_scheduler as stop_bot_detection_scheduler,
)
```

lifespan startup 段（在 liquidation scheduler 之后）加：
```python
await start_bot_detection_scheduler()
```

shutdown 段（在 liquidation 之前）加：
```python
await stop_bot_detection_scheduler()
```

- [ ] **Step 3: 修改 `backend/tests/conftest.py`**

在 `_disable_scheduler` fixture 的 `with` 子句加：
```python
        patch("app.main.start_bot_detection_scheduler", _noop),
        patch("app.main.stop_bot_detection_scheduler", _noop),
```

docstring 改一行提到现在 3 个 scheduler。

- [ ] **Step 4: 跑现有测确认 scheduler patch OK**

`pytest backend/tests/test_bot_detection_main_loop.py -v`
Expected: 4 PASS（没有 scheduler 启动相关报错）

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/bot_detection.py backend/app/main.py backend/tests/conftest.py
git commit -m "feat(bot_detection): APScheduler 接入 main lifespan + conftest patch

bot_detection scheduler 跟 loan/liquidation 同款 lifecycle (start/stop/reschedule)。
max_instances=1 防 tick 重叠。interval 上限 7200 防误配。

conftest _disable_scheduler 加 patch (test 时不真启 3 个 scheduler 任何一个)。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 13: SQLAdmin BotSuspicion ModelView

**Files:**
- Modify: `backend/app/core/admin.py`
- Test: `backend/tests/test_admin_bot_suspicion.py` (新)

- [ ] **Step 1: 写失败测**

`backend/tests/test_admin_bot_suspicion.py`:
```python
import sys, os
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

import pytest


@pytest.mark.asyncio
async def test_sqladmin_bot_suspicion_view_renders(client):
    """SQLAdmin bot_suspicion 列表页能渲染（不抛 500）。"""
    # SQLAdmin 默认路径 /api/v1/admin/<table_identity>/list
    r = await client.get("/api/v1/admin/bot-suspicion/list")
    # 未登录可能 401/302，关键是不能 500
    assert r.status_code in (200, 302, 401), r.text
```

- [ ] **Step 2: 跑确认失败**

`pytest backend/tests/test_admin_bot_suspicion.py -v`
Expected: FAIL（路径不存在或 500）

- [ ] **Step 3: 修改 `backend/app/core/admin.py`**

在文件其他 ModelView 旁加：
```python
from app.models.base import BotSuspicion


class BotSuspicionAdmin(ModelView, model=BotSuspicion):
    name = "Bot 嫌疑"
    name_plural = "Bot 嫌疑记录"
    icon = "fa-solid fa-robot"
    column_list = [
        BotSuspicion.id, BotSuspicion.user_id, BotSuspicion.triggered_at,
        BotSuspicion.signals, BotSuspicion.review_status,
    ]
    column_searchable_list = [BotSuspicion.user_id, BotSuspicion.signals]
    column_sortable_list = [BotSuspicion.triggered_at, BotSuspicion.user_id]
    column_default_sort = [(BotSuspicion.triggered_at, True)]
    column_filters = [BotSuspicion.review_status, BotSuspicion.signals]
    form_columns = [
        BotSuspicion.review_status, BotSuspicion.review_note,
        BotSuspicion.reviewed_by, BotSuspicion.reviewed_at,
    ]
```

在 `setup_admin(app, engine)` 函数内（其他 `admin.add_view(...)` 旁）加：
```python
admin.add_view(BotSuspicionAdmin)
```

- [ ] **Step 4: 跑测**

`pytest backend/tests/test_admin_bot_suspicion.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/core/admin.py backend/tests/test_admin_bot_suspicion.py
git commit -m "feat(admin): SQLAdmin BotSuspicion ModelView (auto-CRUD)

column_list 显示 user_id/triggered_at/signals/review_status；
filter 按 review_status / signals；
form 只暴露 review_* 几个可编辑字段。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 14: 部署配置 + ToS 文档

**Files:**
- Modify: `.env.example`
- Modify: `.github/workflows/ci.yml`
- Create: `docs/tos.md`（如果不存在）/ 否则 Modify

- [ ] **Step 1: 改 `.env.example`**

末尾加（如果还没有）：
```bash
# ── Anti-bot HMAC SECRET (spec 2026-05-20) ──
# 后端验证 X-Client-Token 用；activity_mode_enabled=false 时不验也可空
CLIENT_TOKEN_SECRET=

# 前端 vite build 用 (CI 通过 GitHub Actions secrets 注入)
# VITE_CLIENT_TOKEN_SECRET=<同 CLIENT_TOKEN_SECRET>
```

- [ ] **Step 2: 改 `.github/workflows/ci.yml`**

找 `frontend` job 的 `Build` 步骤：
```yaml
      - name: Build
        run: npm run build
```

改成：
```yaml
      - name: Build
        run: npm run build
        env:
          VITE_CLIENT_TOKEN_SECRET: ${{ secrets.VITE_CLIENT_TOKEN_SECRET }}
```

- [ ] **Step 3: 创建/修改 `docs/tos.md`**

如果文件不存在：
```markdown
# 用户协议

## 1. 服务范围

TouhouCCB 是 LMSR 预测市场学习/娱乐站点，所有"金"币、市场、交易均为虚拟，
不代表任何法定货币或证券。

## 2. 禁止行为

**禁止使用任何形式的自动化脚本/机器人**访问本站 API，包括但不限于：
- 写脚本调用 /api/v1/market/{buy,sell,quote} 等端点
- 使用 headless browser / 自动化工具刷交易
- 多账号联合刷活动奖励
- 利用程序漏洞绕过 anti-bot 检测

违反者本站保留以下处理权（admin 自主决定）：
- 立即封号
- 没收账户内全部"金"币 + 持仓
- 取消活动奖励资格
- 公示用户名

## 3. 异常检测

本站运行行为分析系统，自动识别异常下单模式（高频/凌晨/规律/毫秒级跟进
等）。识别到的可疑账户由 admin 人工 review 后处置。

## 4. 例外

站方运营的量化策略（如 quant trader）经 admin 明示加入白名单后不受
anti-bot 限制。这是站方运营行为不属于用户违规。

## 5. 修订

本协议可能修订；重大改动会站内公告。继续使用即视为接受新版。
```

如果已存在文件，在合适位置追加 "禁止脚本" 章节。

- [ ] **Step 4: Commit**

```bash
git add .env.example .github/workflows/ci.yml docs/tos.md
git commit -m "feat(deploy): anti-bot 配置 + ToS 公示

- .env.example 加 CLIENT_TOKEN_SECRET (后端) + VITE_CLIENT_TOKEN_SECRET 注释
- .github/workflows/ci.yml frontend build 注入 VITE_CLIENT_TOKEN_SECRET
- docs/tos.md 加"禁脚本"条款 + 封号权 + 自动检测说明 + quant 白名单例外

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 15: 全套回归 + 部署 sanity check

**Files:** 全套

- [ ] **Step 1: 全套相关测试**

```bash
cd backend && /data/sunyunbo/www/TouhouCCB/backend/venv/bin/python -m pytest \
  tests/test_bot_suspicion_schema.py \
  tests/test_site_config_anti_bot.py \
  tests/test_settings_anti_bot.py \
  tests/test_anti_bot.py \
  tests/test_market_anti_bot_integration.py \
  tests/test_bot_detection_signals.py \
  tests/test_bot_detection_main_loop.py \
  tests/test_admin_bot_suspicion.py \
  --tb=short -q
```
Expected: 全 PASS (约 30+ test)

- [ ] **Step 2: 跑现有大套回归确认无破坏**

```bash
cd backend && /data/sunyunbo/www/TouhouCCB/backend/venv/bin/python -m pytest \
  tests/test_loan_service.py tests/test_loan_api.py \
  tests/test_liquidation_sweep.py tests/test_liquidation_service.py \
  tests/test_liquidation_admin.py tests/test_liquidation_e2e.py \
  tests/test_liquidation_sweep_perf.py \
  tests/test_user_summary_margin.py tests/test_user_summary_dual_caliber.py \
  tests/test_wealth_mtm.py tests/test_leaderboard.py \
  --tb=short -q
```
Expected: 全 PASS（不应被 anti-bot 改动破坏）

- [ ] **Step 3: Backend 启动 sanity**

```bash
cd backend && /data/sunyunbo/www/TouhouCCB/backend/venv/bin/python -c "import app.main"
```
Expected: 无 import error；可能 log "SECRET_KEY 未配置" 是正常 dev 提示

- [ ] **Step 4: Frontend type-check + lint**

```bash
cd thccb-frontend && /data/sunyunbo/www/TouhouCCB/thccb-frontend/node_modules/.bin/vue-tsc --noEmit
```
Expected: no output

- [ ] **Step 5: Commit "完成" 标志（可选 squash, 或 push）**

```bash
git log --oneline origin/main..HEAD
```
应该有约 14 个 commit。

如果一切 OK：
```bash
git push origin feat/anti-bot
```

- [ ] **Step 6: PR 描述模板**

```markdown
## Summary
- 实施 docs/superpowers/specs/2026-05-20-anti-bot-design.md
- L1 ToS 公示 / L2 HMAC client_token / L3 quant 白名单 / L4 行为监控
- 4 信号：high_freq / late_night / regular_interval / fast_follow
- BotSuspicion 表 + SQLAdmin auto-CRUD
- 默认 activity_mode=false 不影响现有用户，活动当天 admin 一键开

## 部署前 checklist
- [ ] GitHub Actions secrets 加 CLIENT_TOKEN_SECRET + VITE_CLIENT_TOKEN_SECRET
- [ ] 服务器 .env 加 CLIENT_TOKEN_SECRET
- [ ] CI alembic 自动跑 migration

## Test Plan
- [ ] 所有 anti-bot 单测通过 (~30 个)
- [ ] 现有回归通过 (~60 个)
- [ ] 部署后查 SQLAdmin BotSuspicion 列表能进
- [ ] 活动启用前: SQLAdmin → site_config → 加 quant uid + 开 activity_mode
```

---

## Self-Review

**Spec coverage check** (对应 spec 各 section 看是否每个需求都有 task):

- ✅ Architecture overview L1-L4 → Task 1-14 全覆盖
- ✅ Schema BotSuspicion → Task 1, 2
- ✅ Schema 11 site_config keys → Task 3
- ✅ Schema .env CLIENT_TOKEN_SECRET → Task 4
- ✅ L2 verify_client_token → Task 5
- ✅ L2 FastAPI Depends → Task 6
- ✅ L2 前端 clientToken.ts → Task 7
- ✅ L2 axios interceptor → Task 8
- ✅ L4 信号算法 (high_freq, late_night, regular_interval) → Task 9
- ✅ L4 fast_follow 信号 → Task 10
- ✅ L4 主循环 + dedup + 白名单 → Task 11
- ✅ L4 scheduler 接入 main.py + conftest → Task 12
- ✅ SQLAdmin BotSuspicion → Task 13
- ✅ 部署 (.env + ci.yml) + ToS → Task 14
- ✅ 回归 + sanity → Task 15

**Placeholder scan**: 全文搜 "TBD/TODO/implement later/fill in details" → 无。✓

**Type consistency**:
- `verify_client_token(token: str, ts: str, uid: int) -> bool` Task 5 定义，Task 6 一致 ✓
- `parse_whitelist(csv_str: str) -> set[int]` Task 5 / Task 6 / Task 11 一致 ✓
- `BotSuspicion.signals` csv 字符串 (Task 1) / Task 11 用 `",".join(new_signals)` 写入 ✓
- `run_bot_detection_once() -> dict` (Task 11) 返回 dict 字段集跟 Task 12 测试断言一致 ✓
- site_config keys 命名在 Task 3 定义，Task 11 / Task 12 引用名字一致 ✓

**Scope check**: 单一 feature，约 15 个 task，1-2 个工作日实施。✓

**Ambiguity check**:
- Task 6 提到 `site_config.get_str` 可能不存在 → 显式给出"若无加一个"的指令 ✓
- Task 8 interceptor 可能不是 async → 显式说"如果不是，改成 async" ✓
- 测试 `client` fixture 在所有 backend test 中可用（conftest 提供）✓

---

## 执行入口

**Plan complete and saved to `docs/superpowers/plans/2026-05-20-anti-bot.md`. Two execution options:**

**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

**Which approach?**
