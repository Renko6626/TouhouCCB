# SSO P0 修复实施计划（2026-05-12）

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development（subagent 驱动执行）。Steps 用 `- [ ]` checkbox 追踪。

**Goal**：关闭 Phase 1 安全审计发现的 3 个 SSO P0（[P0-AUTH-01/02/03]），杜绝登录固定 / 账户接管攻击面。

**Architecture**：
- 新增 `POST /api/v1/auth/login-start` 端点：服务端生成 state + nonce，写 HttpOnly+Secure+SameSite=Lax cookie，同时返回 body 给前端拼 Casdoor URL
- 改造 `/api/v1/auth/callback`：从 cookie 读 state/nonce，强制比对 + 强制服务端硬编码 `redirect_uri` + 校验 id_token.nonce
- 收紧 `core/oidc.py:verify_token`：加 `issuer=` 校验、移除 `verify_aud:False`、加 `require=[...]` 兜底
- 前端 `casdoor.ts` 改成先 POST `login-start` 拿 state/nonce 再跳 Casdoor；前端 `Callback.vue` 删除 sessionStorage 冗余校验

**Tech Stack**：FastAPI / PyJWT / httpx（后端）+ Vue 3 / TypeScript（前端）。无新依赖。

**红线提示**（CLAUDE.md）：
- `api/v1/auth.py` 和 `core/oidc.py` 是红线文件——本轮**经用户授权改动**；每个 task 在 ralph-log 写明改了什么 + 为什么
- 不动 `.env*`（必要时改 `.env.example` 并在汇报里告诉用户 `.env` 要加什么 key——本计划评估**无需**新增配置项）
- 不 push、不合并到 main、不用 `--no-verify`
- 按文件 `git add`，不用 `-A`

**前置阅读**：
- `docs/security-audit-2026-05-09-p1-core.md` §SSO/Casdoor/Token（发现详情）
- `backend/app/api/v1/auth.py:51-92`（当前 callback 实现）
- `backend/app/core/oidc.py:126-145`（当前 verify_token 实现）
- `thccb-frontend/src/api/casdoor.ts`（当前登录 URL 构造）
- `thccb-frontend/src/pages/auth/Callback.vue:32-38`（当前 sessionStorage state 检查）

**部署侧已知前提**（用户负责）：
- 用户 `.env` 已配置 `CASDOOR_ENDPOINT=https://auth.fantasyguide.cn`
- Casdoor 应用侧已注册回调 URL = `${FRONTEND_URL}/auth/callback`
- 前端和后端是否同源？plan 默认按 SameSite=Lax 写；如果跨域（不同子域），Task 5 验证时切到 SameSite=None

---

## Task 1：建立 fix 分支 + ralph-log 启动条

**前提**：本 plan 已在新建的 `ralph/2026-05-12-sso-fix-p0` 分支上（从 main 切出）。

**Files:**
- Modify: `docs/ralph-log.md`（追加一条）

- [ ] **Step 1：确认分支与工作区**

```bash
git status
git branch --show-current
```

Expected：`ralph/2026-05-12-sso-fix-p0`、工作区只有此 plan 文件未跟踪或已 commit。

- [ ] **Step 2：append ralph-log**

```markdown

## 2026-05-12 HH:MM — SSO P0 修复轮次启动
**目标**：关闭 Phase 1 安全审计 3 个 P0（[P0-AUTH-01/02/03]）
**动机**：账户接管攻击面，登录固定可让受害者用攻击者身份登录站点
**范围**：仅 SSO 三件套——state 服务端校验 + redirect_uri 硬编码 + OIDC iss/aud/nonce 严校
**改动**：本条：仅起分支
**风险 & 回滚**：本条 N/A；后续每个 task 单独标注
**验证**：分支正确（ralph/2026-05-12-sso-fix-p0）
**下一轮**：Task 2 oidc.py verify_token 收紧
```

- [ ] **Step 3：commit**

```bash
git add docs/superpowers/plans/2026-05-12-sso-fix-p0.md docs/ralph-log.md
git commit -m "docs(ralph): SSO P0 修复轮次启动（plan + log）"
```

---

## Task 2：`core/oidc.py` 收紧 verify_token

**Files:**
- Modify: `backend/app/core/oidc.py:126-146`

**为什么**：当前 `jwt.decode(..., options={"verify_aud": False})` + 未传 `issuer=`，让伪造或跨应用 token 能通过校验。

- [ ] **Step 1：修改 verify_token**

把 `verify_token` 改为：

```python
def verify_token(self, token: str, *, nonce: Optional[str] = None) -> Dict[str, Any]:
    """
    用 JWKS 公钥验证并解码 JWT，返回 claims dict。
    强校验 issuer / audience / 必需 claims；可选校验 nonce（id_token）。
    """
    if not self._jwk_client:
        raise RuntimeError("OIDC 客户端未初始化，请先调用 ensure_ready()")

    signing_key: PyJWK = self._jwk_client.get_signing_key_from_jwt(token)

    claims = jwt.decode(
        token,
        signing_key.key,
        algorithms=["RS256", "ES256"],
        audience=self.audience,
        issuer=self.issuer_url,
        options={
            "require": ["exp", "iat", "iss", "aud", "sub"],
        },
    )

    if nonce is not None:
        token_nonce = claims.get("nonce")
        if not token_nonce or token_nonce != nonce:
            raise jwt.InvalidTokenError("nonce 不匹配")

    return claims
```

并在文件顶部已有的 `from typing import Any, Dict, Optional` 之外不需要加 import（`Optional` 已存在）。

- [ ] **Step 2：手动检查 imports 与签名一致**

确认 `Optional` 在 import 列表里（应该已经在）。

- [ ] **Step 3：commit**

```bash
git add backend/app/core/oidc.py
git commit -m "$(cat <<'EOF'
fix(oidc): 强校验 iss/aud/require + 支持 nonce（P0-AUTH-01）

- 移除 options={"verify_aud": False}，让 PyJWT 用 audience= 真去校验
- 加 issuer=self.issuer_url，强校验签发方
- 加 options={"require": [...exp,iat,iss,aud,sub]}，兜住缺 claim 边缘 token
- 加可选 nonce 参数，给 callback 校验 id_token.nonce 用

红线文件改动经用户授权（docs/security-audit-2026-05-09-p1-core.md P0-AUTH-01）。
EOF
)"
```

---

## Task 3：`api/v1/auth.py` — 加 `/login-start`、改 `/callback`

**Files:**
- Modify: `backend/app/api/v1/auth.py`

**为什么**：state 不校验 + redirect_uri 客户端可控（P0-AUTH-02 / P0-AUTH-03）+ 配合 Task 2 的 nonce 校验。

- [ ] **Step 1：在 auth.py 顶部加 import**

```python
import secrets
from fastapi import APIRouter, Depends, HTTPException, Request, Response, status
```

`secrets` 和 `Request`/`Response` 是新加的。

- [ ] **Step 2：加常量**

在 router 定义后加：

```python
_STATE_COOKIE = "thccb_oauth_state"
_NONCE_COOKIE = "thccb_oauth_nonce"
_COOKIE_MAX_AGE = 300  # 5 分钟，足以走完 Casdoor 登录页
```

- [ ] **Step 3：加 `/login-start` 端点**

在 `oauth_callback` 之前插入：

```python
class LoginStartResponse(BaseModel):
    state: str
    nonce: str


@router.post("/login-start", summary="生成 OAuth state/nonce 并写 HttpOnly cookie")
async def login_start(response: Response):
    """
    前端跳转到 Casdoor 之前必须先调此接口。
    服务端生成随机 state + nonce，写入 HttpOnly cookie，并把同值返回给前端拼到
    Casdoor authorize URL 里。callback 时服务端从 cookie 读取并比对。
    """
    state = secrets.token_urlsafe(32)
    nonce = secrets.token_urlsafe(32)

    cookie_kwargs = dict(
        max_age=_COOKIE_MAX_AGE,
        httponly=True,
        secure=settings.FRONTEND_URL.startswith("https://"),
        samesite="lax",
        path="/api/v1/auth",
    )
    response.set_cookie(_STATE_COOKIE, state, **cookie_kwargs)
    response.set_cookie(_NONCE_COOKIE, nonce, **cookie_kwargs)

    return LoginStartResponse(state=state, nonce=nonce)
```

- [ ] **Step 4：改 `CallbackRequest`，去掉 redirect_uri 字段**

```python
class CallbackRequest(BaseModel):
    code: str
    state: str  # 必填，原 Optional 改为必填
```

`redirect_uri` 字段彻底删除——服务端不再接受客户端提交的回跳地址。

- [ ] **Step 5：重写 `oauth_callback`**

把现有 `oauth_callback` 整个函数替换为：

```python
@router.post("/callback", summary="Casdoor OAuth2 回调 — 换取本站 JWT")
async def oauth_callback(
    body: CallbackRequest,
    request: Request,
    response: Response,
    db: AsyncSession = Depends(get_async_session),
):
    """
    前端在 /auth/callback 页面拿到 Casdoor 返回的 code，POST 到此接口。
    1. 校验 state：cookie 中存的 state 必须等于 body 提交的 state（CSRF / login fixation 防护）
    2. 用 code + 服务端硬编码 redirect_uri 向 Casdoor 换 token
    3. 用 JWKS 验证 id_token 签名 + iss + aud + nonce
    4. 查找或创建本站用户记录（首次登录自动开户）
    5. 颁发本站 JWT，清掉 state/nonce cookie
    """
    # 1. 校验 state（防 CSRF）
    cookie_state = request.cookies.get(_STATE_COOKIE)
    cookie_nonce = request.cookies.get(_NONCE_COOKIE)

    if not cookie_state or not body.state or not secrets.compare_digest(cookie_state, body.state):
        # 清掉可能过期的 cookie，防止误导
        response.delete_cookie(_STATE_COOKIE, path="/api/v1/auth")
        response.delete_cookie(_NONCE_COOKIE, path="/api/v1/auth")
        raise HTTPException(
            status_code=status.HTTP_400_BAD_REQUEST,
            detail="state 校验失败，请重新登录",
        )

    if not cookie_nonce:
        response.delete_cookie(_STATE_COOKIE, path="/api/v1/auth")
        response.delete_cookie(_NONCE_COOKIE, path="/api/v1/auth")
        raise HTTPException(
            status_code=status.HTTP_400_BAD_REQUEST,
            detail="nonce 缺失，请重新登录",
        )

    oidc = _get_oidc()

    # redirect_uri 服务端硬编码，不接受客户端提交（防开放重定向链）
    redirect_uri = f"{settings.FRONTEND_URL}/auth/callback"

    try:
        await oidc.ensure_ready()
        token_resp = await oidc.exchange_code(body.code, redirect_uri)
    except Exception as e:
        logger.error("OIDC token exchange failed: %s: %s", type(e).__name__, e)
        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail="认证失败，请重试")

    # 必须用 id_token（含 nonce），不再 fallback access_token
    raw_token = token_resp.get("id_token")
    if not raw_token:
        logger.error("Token response missing id_token")
        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail="认证服务返回异常")

    try:
        claims = oidc.verify_token(raw_token, nonce=cookie_nonce)
    except Exception as e:
        logger.error("JWT verification failed: %s: %s", type(e).__name__, e)
        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail="令牌验证失败，请重试")

    casdoor_id: str = claims.get("sub", "")
    if not casdoor_id:
        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail="令牌中缺少用户标识")

    username: str = claims.get("name") or claims.get("preferred_username") or casdoor_id
    email: Optional[str] = claims.get("email") or None

    # 查找或创建本站用户
    result = await db.execute(select(User).where(User.casdoor_id == casdoor_id))
    user: Optional[User] = result.scalars().first()

    if not user:
        async with managed_transaction(db):
            user_count = await db.execute(select(func.count()).select_from(User))
            is_first_user = user_count.scalar_one() == 0

            base = username
            suffix = 0
            while True:
                name_check = await db.execute(select(User).where(User.username == username))
                if not name_check.scalars().first():
                    break
                suffix += 1
                username = f"{base}_{suffix}"

            user = User(
                casdoor_id=casdoor_id,
                username=username,
                email=email,
                cash=Decimal(str(settings.INITIAL_BALANCE)),
                debt=Decimal("0"),
                is_active=True,
                is_superuser=is_first_user,
            )
            db.add(user)
            await db.flush()

            if is_first_user:
                logger.info("First user registered as admin: %s (id=%s)", username, user.id)
    elif not user.is_active:
        raise HTTPException(status_code=status.HTTP_403_FORBIDDEN, detail="账号已被禁用")

    # 校验成功，清掉一次性 cookie
    response.delete_cookie(_STATE_COOKIE, path="/api/v1/auth")
    response.delete_cookie(_NONCE_COOKIE, path="/api/v1/auth")

    return {
        "access_token": create_access_token(user.id),
        "refresh_token": create_refresh_token(user.id),
        "token_type": "bearer",
    }
```

- [ ] **Step 6：py_compile 自检**

```bash
cd /data/sunyunbo/www/TouhouCCB/backend
python -m py_compile app/api/v1/auth.py
python -c "import app.api.v1.auth"
```

Expected：无报错。如有 ImportError / SyntaxError，停下排查。

- [ ] **Step 7：commit**

```bash
git add backend/app/api/v1/auth.py
git commit -m "$(cat <<'EOF'
fix(auth): 服务端校验 state/nonce + 硬编码 redirect_uri（P0-AUTH-02/03）

- 新增 POST /api/v1/auth/login-start：服务端生成 state+nonce，写 HttpOnly cookie
- /callback：从 cookie 校验 state（CSRF 防护）+ id_token.nonce 校验
- 删除 body.redirect_uri 字段；服务端硬编码 ${FRONTEND_URL}/auth/callback
- 仅用 id_token，不再 fallback access_token（封 P1-AUTH-05 衍生面）
- 用 secrets.compare_digest 做常量时间比对

红线文件改动经用户授权（docs/security-audit-2026-05-09-p1-core.md P0-AUTH-02/03）。
EOF
)"
```

---

## Task 4：前端 `casdoor.ts` — 改造登录入口走 login-start

**Files:**
- Modify: `thccb-frontend/src/api/casdoor.ts`

**为什么**：前端不再单方面生成 state，改为先调后端拿；后端 cookie 完成 CSRF 防御。

- [ ] **Step 1：定位 axios / fetch 实例**

```bash
grep -rn "axios\|baseURL\|VITE_API_BASE" thccb-frontend/src/api/ thccb-frontend/src/utils/ thccb-frontend/src/composables/ 2>/dev/null | head -20
```

记录前端 HTTP 客户端文件（通常是 `src/api/index.ts` 或 `src/utils/http.ts`）。

- [ ] **Step 2：确认 axios `withCredentials`**

打开前端 HTTP 客户端文件，确认 `axios.create({ ..., withCredentials: true })`。如果没有，加上——否则后端 set-cookie 会被丢。

若需修改 HTTP 客户端，先 commit 这一步：

```bash
git add thccb-frontend/src/<path-to-http-client>.ts
git commit -m "fix(api): 启用 withCredentials 让 oauth state cookie 在跨域 fetch 时被携带"
```

- [ ] **Step 3：改 `casdoor.ts`**

把当前文件替换为：

```typescript
/**
 * Casdoor OAuth2 登录 — 服务端生成 state/nonce，前端只负责跳转。
 *
 * 标准 OAuth2 Authorization Code 流程：
 *   1. 前端调 POST /api/v1/auth/login-start
 *      → 后端生成 state+nonce，写 HttpOnly cookie，body 同值返回
 *   2. 前端拼 authorize URL（带 state + nonce）→ 跳转到 Casdoor 登录页
 *   3. 用户登录后 Casdoor 重定向回 /auth/callback?code=xxx&state=yyy
 *   4. 前端把 code+state 发给后端换本站 JWT
 *      → 后端从 cookie 校验 state + id_token.nonce
 */

import http from './index'  // 或者实际的 axios 实例路径

const CASDOOR_URL = import.meta.env.VITE_CASDOOR_URL as string
const CLIENT_ID = import.meta.env.VITE_CASDOOR_CLIENT_ID as string
const ORG_NAME = import.meta.env.VITE_CASDOOR_ORG as string
const APP_NAME = import.meta.env.VITE_CASDOOR_APP as string

const REQUIRED = { VITE_CASDOOR_URL: CASDOOR_URL, VITE_CASDOOR_CLIENT_ID: CLIENT_ID, VITE_CASDOOR_ORG: ORG_NAME, VITE_CASDOOR_APP: APP_NAME }
for (const [key, val] of Object.entries(REQUIRED)) {
  if (!val) console.error(`[Casdoor] 缺少环境变量: ${key}`)
}

const REDIRECT_URI = `${window.location.origin}/auth/callback`

interface LoginStartResponse {
  state: string
  nonce: string
}

/** 调后端 login-start 拿 state+nonce（后端同时写 HttpOnly cookie） */
async function fetchStateAndNonce(): Promise<LoginStartResponse> {
  const resp = await http.post<LoginStartResponse>('/auth/login-start')
  return resp.data
}

/** 拼接 Casdoor OAuth2 authorize URL */
async function buildAuthorizeUrl(type: 'login' | 'signup'): Promise<string> {
  const { state, nonce } = await fetchStateAndNonce()
  const params = new URLSearchParams({
    client_id: CLIENT_ID,
    response_type: 'code',
    redirect_uri: REDIRECT_URI,
    scope: 'openid profile email',
    state,
    nonce,
  })
  const path = type === 'signup'
    ? `/signup/${APP_NAME}`
    : `/login/oauth/authorize`
  return `${CASDOOR_URL}${path}?${params.toString()}`
}

export const getLoginUrl = () => buildAuthorizeUrl('login')
export const getRegisterUrl = () => buildAuthorizeUrl('signup')
```

**关键点**：
- 函数签名从同步 `string` 改为 `async Promise<string>`——调用方需要 `await`
- 删除 `generateState()` 和 sessionStorage 操作
- import path `'./index'` 假设；如果实际路径不同，调整

- [ ] **Step 4：找所有 `getLoginUrl` / `getRegisterUrl` 调用方，确保 `await`**

```bash
grep -rn "getLoginUrl\|getRegisterUrl" thccb-frontend/src/
```

每个调用方：原来 `window.location.href = getLoginUrl()` 改成 `window.location.href = await getLoginUrl()`，并把外层函数标 `async`。

- [ ] **Step 5：commit**

```bash
git add thccb-frontend/src/api/casdoor.ts
# + 任何调用方文件
git commit -m "$(cat <<'EOF'
fix(frontend): 登录入口先调 /auth/login-start，由服务端生成 state/nonce

- 前端不再自行生成 state；服务端 cookie 完成 CSRF 防御
- getLoginUrl/getRegisterUrl 改为 async（调用方需 await）
- nonce 也带进 authorize URL，配合后端 id_token nonce 校验

配合 backend/api/v1/auth.py 的 /login-start + /callback 改造。
EOF
)"
```

---

## Task 5：前端 `Callback.vue` — 删除冗余 sessionStorage state 校验

**Files:**
- Modify: `thccb-frontend/src/pages/auth/Callback.vue`

**为什么**：后端已通过 cookie 强校验 state；前端 sessionStorage 那套既绕得过去，又会和新流程冲突（新流程前端从未把 state 写进 sessionStorage）。

- [ ] **Step 1：删除 sessionStorage 校验段**

把 `onMounted` 中的：

```javascript
// 校验 OAuth2 state 参数，防止 CSRF
const savedState = sessionStorage.getItem('oauth_state')
sessionStorage.removeItem('oauth_state')
if (!savedState || !state || state !== savedState) {
  error.value = '安全校验失败（state 不匹配），请重新登录。'
  return
}
```

整段删除。`state` 变量保留（要传给后端）。

- [ ] **Step 2：commit**

```bash
git add thccb-frontend/src/pages/auth/Callback.vue
git commit -m "fix(frontend): 删除冗余的前端 state 校验（后端 cookie 已接管）"
```

---

## Task 6：后端测试

**Files:**
- Create: `backend/tests/test_auth_state_nonce.py`

**为什么**：新加入的安全防御如果没有测试覆盖，未来重构容易被静默回退。

- [ ] **Step 1：先看现有 test_auth.py 结构**

```bash
cat backend/tests/test_auth.py | head -80
```

理解 fixture / client 习惯，让新测试风格一致。

- [ ] **Step 2：写测试**

创建 `backend/tests/test_auth_state_nonce.py`，至少覆盖：

```python
"""SSO P0 修复后的安全测试：state / nonce / redirect_uri 强校验。"""
import pytest


@pytest.mark.asyncio
async def test_login_start_sets_state_and_nonce_cookies(client):
    """/login-start 必须返回 state+nonce 并写 HttpOnly cookie。"""
    r = await client.post("/api/v1/auth/login-start")
    assert r.status_code == 200
    body = r.json()
    assert "state" in body and len(body["state"]) >= 32
    assert "nonce" in body and len(body["nonce"]) >= 32
    cookies = r.cookies
    assert "thccb_oauth_state" in cookies
    assert "thccb_oauth_nonce" in cookies


@pytest.mark.asyncio
async def test_callback_rejects_missing_state_cookie(client):
    """没有 state cookie 直接 POST /callback 必须 400。"""
    r = await client.post(
        "/api/v1/auth/callback",
        json={"code": "fake_code", "state": "anything"},
    )
    assert r.status_code == 400
    assert "state" in r.json()["detail"]


@pytest.mark.asyncio
async def test_callback_rejects_state_mismatch(client):
    """cookie 有 state，但 body 提交不一致必须 400。"""
    # 先调 login-start 拿 cookie
    await client.post("/api/v1/auth/login-start")
    r = await client.post(
        "/api/v1/auth/callback",
        json={"code": "fake_code", "state": "wrong_state_xxx"},
    )
    assert r.status_code == 400


@pytest.mark.asyncio
async def test_callback_request_no_longer_accepts_redirect_uri(client):
    """body 即使带 redirect_uri，服务端也必须忽略——pydantic schema 不接受。"""
    await client.post("/api/v1/auth/login-start")
    # state 取一个能过第一关的，但 code 会失败——重点是 schema 校验
    r = await client.post(
        "/api/v1/auth/callback",
        json={"code": "fake", "state": "fake", "redirect_uri": "https://evil.com/cb"},
    )
    # Pydantic 默认会忽略额外字段（model_config 默认），所以这条会进到 state 校验失败
    # 此测试主要验证：即使 extra 字段存在，行为不变
    assert r.status_code == 400
```

如果项目用同步 TestClient 而非 async，按现有 `test_auth.py` 风格改。

- [ ] **Step 3：跑测试**

```bash
cd /data/sunyunbo/www/TouhouCCB/backend
pytest tests/test_auth_state_nonce.py -v
```

Expected：4 个测试全过。

- [ ] **Step 4：跑全套 auth 测试，确认没破其他**

```bash
pytest tests/test_auth.py -v
```

如果有原有测试因为流程变了而 fail（比如老测试调 `/callback` 不带 cookie），需要更新老测试——但**仅限测试文件**，产品代码不再改。

- [ ] **Step 5：commit**

```bash
git add backend/tests/test_auth_state_nonce.py
# + 任何更新的老测试
git commit -m "test(auth): SSO P0 修复后的 state/nonce 强校验测试覆盖"
```

---

## Task 7：完整验证 — type-check / lint / pytest / 手测

**Files:**
- Modify: `docs/ralph-log.md`（追加验证记录）

- [ ] **Step 1：后端语法 + import 检查**

```bash
cd /data/sunyunbo/www/TouhouCCB/backend
python -m py_compile $(find app -name '*.py')
python -c "import app.main"
```

Expected：无报错。

- [ ] **Step 2：后端 pytest 全套**

```bash
pytest -x
```

Expected：全过。任何 fail 必须分析根因——禁止 `--no-verify` 或屏蔽测试。

- [ ] **Step 3：前端 type-check + lint**

```bash
cd /data/sunyunbo/www/TouhouCCB/thccb-frontend
npm run type-check
npm run lint
```

Expected：全过。

- [ ] **Step 4：手测**

如果开发环境能起，启动后端 + 前端：
- 用浏览器走一次完整 SSO 登录流程
- DevTools Network 查看：`/auth/login-start` 返回 200 + 设了两个 cookie
- 跳 Casdoor → 登录 → 回跳 → `/auth/callback` POST 返回 200 + access_token
- DevTools Application > Cookies：state/nonce cookie 在 callback 后被清除

如果环境起不来，在 ralph-log 明写"未实测 UI"。

- [ ] **Step 5：append ralph-log**

```markdown

## 2026-05-12 HH:MM — SSO P0 修复验证
**目标**：验证 Task 2-6 改动
**改动**：本条仅文档
**验证**：
- py_compile ✅ / import ✅
- pytest 全套：N pass / M fail（如有 fail 列原因）
- 前端 type-check ✅ / lint ✅
- 手测：完整登录 ✅ / cookie 写入清除 ✅ / state 篡改 400 ✅
  - （或：未实测 UI，原因 XXX）
**下一轮**：Task 8 收尾汇报
```

- [ ] **Step 6：commit**

```bash
git add docs/ralph-log.md
git commit -m "docs(ralph): SSO P0 修复验证记录"
```

---

## Task 8：收尾 + 汇报

**Files:**
- Modify: `docs/ralph-log.md`（最终条）

- [ ] **Step 1：append 最终 ralph-log**

```markdown

## 2026-05-12 HH:MM — SSO P0 修复完成
**目标**：P0-AUTH-01/02/03 修复全部就位
**范围**：跨前后端共 ~6 commit
**改动汇总**：
- `backend/app/core/oidc.py` — verify_token 收紧 iss/aud/require/nonce
- `backend/app/api/v1/auth.py` — 新增 /login-start，重写 /callback（state cookie + nonce 校验 + 硬编码 redirect_uri）
- `thccb-frontend/src/api/casdoor.ts` — async 登录 URL，先调 login-start
- `thccb-frontend/src/pages/auth/Callback.vue` — 删冗余 sessionStorage 校验
- `backend/tests/test_auth_state_nonce.py` — 新测试覆盖
**风险 & 回滚**：分支 `ralph/2026-05-12-sso-fix-p0` 独立未 push；回滚 = checkout main
**待用户操作**：
1. Casdoor 应用配置侧确认 redirect_uri 白名单只有 `${FRONTEND_URL}/auth/callback`
2. 部署到 staging/test 环境跑一遍完整流程
3. 决定何时合并 main（push = 自动部署上线）
**下一轮**：等用户决定（合并 main / 起 P1 修复轮次 / 进 Phase 2 审计）
```

- [ ] **Step 2：commit**

```bash
git add docs/ralph-log.md
git commit -m "docs(ralph): SSO P0 修复轮次收尾"
```

- [ ] **Step 3：汇报给用户**

按 CLAUDE.md "每轮结束一句话"格式：
- 改了什么（5 个文件 + 1 个新测试）
- 在哪个分支（`ralph/2026-05-12-sso-fix-p0`，未 push）
- 验证结果（type-check/lint/pytest 结果 + 手测状态）
- 待用户决定（Casdoor 配置核查 + 合并时机）

**不主动 push、不主动 merge、不主动起新轮次。**

---

## Self-Review 检查（plan 自查）

- [x] **占位符扫描**：无 TBD / TODO。每个 step 给了具体命令或代码块
- [x] **类型一致性**：`_STATE_COOKIE`、`_NONCE_COOKIE`、`/auth/login-start` 路径、cookie 字段名跨文件一致
- [x] **红线遵守**：`.env*` 不动；`api/v1/auth.py`、`core/oidc.py` 改动经用户授权，每个 commit 引用 finding ID
- [x] **回滚路径**：独立分支、不 push；如果 type-check / pytest fail，停下不强推
- [x] **跨域问题**：plan 默认 SameSite=Lax；Task 4 Step 2 提到 axios `withCredentials`；Task 7 手测验证一切

## 假设与未决项

1. **前端 axios baseURL 与后端是否同源**：plan 假设同源（nginx 反代），SameSite=Lax 工作。如果跨域，Task 4 Step 2 需要切到 SameSite=None+Secure，并且 CORS `allow_credentials=True` 已经在。
2. **Casdoor 应用是否在白名单了 nonce 参数**：标准 OIDC 应用应该支持；如果 Casdoor 版本不返回 id_token.nonce，Task 2 的 nonce 校验会 fail——届时降级为软校验（仅 warn）并在 Task 8 汇报里标注。
3. **前端 axios 实例的具体路径**：Task 4 Step 1 用 grep 定位；如果文件结构不对，子 agent 应停下问。
