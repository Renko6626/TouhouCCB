# LoanV1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现基于复利 + APS 定时 sweep 的贷款系统：玩家 borrow/repay、管理员强制放贷/免债、运行时可调的 SiteConfig。

**Architecture:** `User.debt` 复利累积在一个字段里，不分本息。APS 每 60s（可配）扫全体欠债用户结息；所有动债务的业务路径（buy/sell/borrow/repay/force-loan/forgive-debt）先 `accrue_interest` 再动账。核心原子操作抽到 `loan_service.py`，API handler 退化为薄封装。

**Tech Stack:** FastAPI + SQLModel (Postgres) + APScheduler + Vue 3 + Pinia。测试：pytest-asyncio + httpx AsyncClient（复用 `tests/test_auth.py` fixture 模式）。

**Spec:** `docs/superpowers/specs/2026-04-24-loan-design.md`

**Branch:** 继续在 `ralph/2026-04-24-loan-spec` 或新开 `ralph/2026-04-24-loan-impl`（推荐后者，spec 与实现独立可回滚）。

---

## File Structure

**新建后端文件：**
- `backend/app/services/site_config.py` — SiteConfig 读写 + 缓存
- `backend/app/services/loan_service.py` — 四个原子函数
- `backend/app/services/loan_sweep.py` — APS 定时结息
- `backend/app/api/v1/loan.py` — 玩家 `/loan/*` 接口
- `backend/app/api/v1/site_config.py` — 超管 `/admin/site-config/*` 接口
- `backend/app/schemas/loan.py` — 请求/响应模型
- `backend/scripts/migrate_loan_v1.py` — 幂等迁移脚本
- `backend/tests/test_loan_service.py` / `test_loan_sweep.py` / `test_loan_api.py` / `test_loan_admin.py` / `test_site_config.py`

**新建前端文件：**
- `thccb-frontend/src/pages/loan/Loan.vue` — 玩家借还页
- `thccb-frontend/src/pages/admin/SiteConfig.vue` — 管理员站点配置页
- `thccb-frontend/src/stores/loan.ts` — Pinia store
- `thccb-frontend/src/api/loan.ts` — API client

**修改的文件：**
- `backend/app/models/base.py` — User 加 `debt_last_accrued_at`，新增 `SiteConfig` 模型
- `backend/app/main.py` — 挂载两个新路由，lifespan 启动 sweep
- `backend/app/api/v1/market.py` — `buy` / `sell` 在锁行后调 `accrue_interest`
- `backend/app/api/v1/user.py` — `adjust-cash` 加 accrue；新增 `/admin/user/{id}/force-loan` 和 `/admin/user/{id}/forgive-debt`
- `backend/app/core/users.py` — 如需导出 `current_superuser` 别名（已存在则跳过）
- `backend/requirements.txt` — 加 `APScheduler`
- `thccb-frontend/src/router/routes.ts` — 加 `/loan` 路由
- `thccb-frontend/src/layouts/DefaultLayout.vue` — Header 负债徽章
- `thccb-frontend/src/api/index.ts` — 导出 loan API
- `docs/deploy.md` — LoanV1 上线步骤
- `docs/ralph-log.md` — 每阶段追加一条

---

## Testing Conventions

所有后端测试沿用 `backend/tests/test_auth.py` 的 fixture 模式：
```python
import sys, os
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))
import pytest, pytest_asyncio, uuid
from decimal import Decimal
from httpx import AsyncClient
from asgi_lifespan import LifespanManager
from app.main import app
from app.core.database import engine, async_session_maker
from app.core.users import create_access_token
from app.models.base import User
from sqlmodel import SQLModel

@pytest_asyncio.fixture(autouse=True)
async def setup_db():
    async with engine.begin() as conn:
        await conn.run_sync(SQLModel.metadata.create_all)

@pytest_asyncio.fixture
async def client():
    async with LifespanManager(app):
        async with AsyncClient(base_url="http://localhost:8004", app=app) as ac:
            yield ac
```

**运行命令**（在 `backend/` 目录）：
```bash
source venv/bin/activate
pytest tests/<file> -x -v
```

---

## Task 1: 加 APScheduler 依赖

**Files:**
- Modify: `backend/requirements.txt`

- [ ] **Step 1: 追加依赖**

在 `backend/requirements.txt` 末尾追加一行：
```
APScheduler==3.10.4
```

- [ ] **Step 2: 安装并验证**

```bash
cd backend && source venv/bin/activate && pip install APScheduler==3.10.4
python -c "from apscheduler.schedulers.asyncio import AsyncIOScheduler; print('ok')"
```
Expected: 输出 `ok`

- [ ] **Step 3: Commit**

```bash
git add backend/requirements.txt
git commit -m "chore(backend): 加 APScheduler==3.10.4 依赖（LoanV1 sweep）"
```

---

## Task 2: 数据模型加字段 + SiteConfig 表

**Files:**
- Modify: `backend/app/models/base.py`

- [ ] **Step 1: User 加 debt_last_accrued_at**

在 `backend/app/models/base.py` 的 `User` 类里，`debt` 字段下方加：
```python
    # LoanV1 — 上次利息结算时间；debt=0 时为 None
    debt_last_accrued_at: Optional[datetime] = Field(
        default=None,
        sa_type=DateTime(timezone=True),
    )
```

- [ ] **Step 2: 文件末尾追加 SiteConfig 模型**

```python
class SiteConfig(SQLModel, table=True):
    """站点 key-value 配置表，超管可运行时修改。"""
    __table_args__ = (
        UniqueConstraint("key", name="uq_siteconfig_key"),
    )

    id: Optional[int] = Field(default=None, primary_key=True)
    key: str = Field(index=True, nullable=False)
    value: str = Field(nullable=False)
    value_type: str = Field(nullable=False)  # "decimal" | "int" | "bool"
    updated_at: datetime = Field(
        default_factory=lambda: datetime.now(timezone.utc),
        sa_type=DateTime(timezone=True),
    )
    updated_by: Optional[int] = Field(default=None, foreign_key="user.id")
```

- [ ] **Step 3: 类型检查**

```bash
cd backend && python -m py_compile app/models/base.py && python -c "from app.models.base import User, SiteConfig; print(User.__table__.columns.keys()); print(SiteConfig.__table__.columns.keys())"
```
Expected: User 列表包含 `debt_last_accrued_at`；SiteConfig 列表包含 `key`/`value`/`value_type`/`updated_at`/`updated_by`

- [ ] **Step 4: Commit**

```bash
git add backend/app/models/base.py
git commit -m "feat(backend): User 加 debt_last_accrued_at + 新增 SiteConfig 模型"
```

---

## Task 3: 迁移脚本

**Files:**
- Create: `backend/scripts/migrate_loan_v1.py`

- [ ] **Step 1: 写脚本**

```python
"""LoanV1 幂等迁移脚本。
- User.debt_last_accrued_at 列
- SiteConfig 表 + 默认配置
- 防御性兜底：debt > 0 但 last_accrued_at 为空的用户补 now()
运行：python -m scripts.migrate_loan_v1（在 backend/ 目录，激活 venv 后）
"""
import asyncio
import sys, os
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

from sqlalchemy import text
from app.core.database import engine


DEFAULT_CONFIGS = [
    ("loan_enabled", "true", "bool"),
    ("loan_leverage_k", "1.0", "decimal"),
    ("loan_daily_rate", "0.01", "decimal"),
    ("loan_sweep_interval_sec", "60", "int"),
]


async def run():
    async with engine.begin() as conn:
        # 1. User 加列
        await conn.execute(text(
            'ALTER TABLE "user" ADD COLUMN IF NOT EXISTS debt_last_accrued_at TIMESTAMPTZ NULL'
        ))

        # 2. SiteConfig 表
        await conn.execute(text("""
            CREATE TABLE IF NOT EXISTS siteconfig (
                id SERIAL PRIMARY KEY,
                key VARCHAR(64) NOT NULL,
                value TEXT NOT NULL,
                value_type VARCHAR(16) NOT NULL,
                updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
                updated_by INTEGER REFERENCES "user"(id)
            )
        """))
        await conn.execute(text(
            'CREATE UNIQUE INDEX IF NOT EXISTS uq_siteconfig_key ON siteconfig(key)'
        ))

        # 3. 默认配置
        for k, v, t in DEFAULT_CONFIGS:
            await conn.execute(
                text("INSERT INTO siteconfig (key, value, value_type) VALUES (:k, :v, :t) ON CONFLICT (key) DO NOTHING"),
                {"k": k, "v": v, "t": t},
            )

        # 4. 兜底
        await conn.execute(text(
            'UPDATE "user" SET debt_last_accrued_at = NOW() WHERE debt > 0 AND debt_last_accrued_at IS NULL'
        ))

    print("migrate_loan_v1: done")


if __name__ == "__main__":
    asyncio.run(run())
```

- [ ] **Step 2: 语法检查**

```bash
cd backend && python -m py_compile scripts/migrate_loan_v1.py
```
Expected: 无输出（无语法错）

- [ ] **Step 3: Commit**

⚠️ 本脚本会操作生产 DB，**不在本阶段运行**，等整体 PR 合并前由人工在 staging 运行一次。

```bash
mkdir -p backend/scripts && touch backend/scripts/__init__.py
git add backend/scripts/__init__.py backend/scripts/migrate_loan_v1.py
git commit -m "feat(backend): LoanV1 幂等迁移脚本（未运行，待合并前人工执行）"
```

---

## Task 4: SiteConfig 服务（TDD）

**Files:**
- Create: `backend/tests/test_site_config.py`
- Create: `backend/app/services/site_config.py`

- [ ] **Step 1: 写失败测试**

`backend/tests/test_site_config.py`：
```python
import sys, os
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

import pytest, pytest_asyncio
from decimal import Decimal
from sqlmodel import SQLModel
from app.core.database import engine, async_session_maker
from app.models.base import SiteConfig
from app.services.site_config import (
    get_decimal, get_int, get_bool, set_value, SiteConfigError,
)


@pytest_asyncio.fixture(autouse=True)
async def setup_db():
    async with engine.begin() as conn:
        await conn.run_sync(SQLModel.metadata.create_all)
    async with async_session_maker() as s:
        async with s.begin():
            s.add(SiteConfig(key="x_rate", value="0.05", value_type="decimal"))
            s.add(SiteConfig(key="x_interval", value="30", value_type="int"))
            s.add(SiteConfig(key="x_flag", value="true", value_type="bool"))


@pytest.mark.asyncio
async def test_get_decimal_returns_decimal():
    async with async_session_maker() as s:
        v = await get_decimal(s, "x_rate")
    assert v == Decimal("0.05")


@pytest.mark.asyncio
async def test_get_int_returns_int():
    async with async_session_maker() as s:
        v = await get_int(s, "x_interval")
    assert v == 30


@pytest.mark.asyncio
async def test_get_bool_true_false():
    async with async_session_maker() as s:
        v = await get_bool(s, "x_flag")
    assert v is True


@pytest.mark.asyncio
async def test_missing_key_raises():
    async with async_session_maker() as s:
        with pytest.raises(SiteConfigError):
            await get_decimal(s, "nope")


@pytest.mark.asyncio
async def test_set_value_updates_row():
    async with async_session_maker() as s:
        await set_value(s, "x_rate", "0.1", admin_user_id=None)
    async with async_session_maker() as s:
        v = await get_decimal(s, "x_rate")
    assert v == Decimal("0.1")
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd backend && source venv/bin/activate && pytest tests/test_site_config.py -x -v
```
Expected: FAIL — `ModuleNotFoundError: No module named 'app.services.site_config'`

- [ ] **Step 3: 写实现**

`backend/app/services/site_config.py`：
```python
"""SiteConfig 读写服务。值按 value_type 解析。"""
from __future__ import annotations
from decimal import Decimal
from datetime import datetime, timezone
from typing import Optional

from sqlalchemy.ext.asyncio import AsyncSession
from sqlmodel import select

from app.models.base import SiteConfig


class SiteConfigError(Exception):
    pass


async def _fetch(session: AsyncSession, key: str) -> SiteConfig:
    row = (await session.exec(select(SiteConfig).where(SiteConfig.key == key))).first()
    if row is None:
        raise SiteConfigError(f"siteconfig key not found: {key}")
    return row


async def get_decimal(session: AsyncSession, key: str) -> Decimal:
    row = await _fetch(session, key)
    return Decimal(row.value)


async def get_int(session: AsyncSession, key: str) -> int:
    row = await _fetch(session, key)
    return int(row.value)


async def get_bool(session: AsyncSession, key: str) -> bool:
    row = await _fetch(session, key)
    return row.value.lower() in ("true", "1", "yes")


async def get_all(session: AsyncSession) -> list[SiteConfig]:
    return list((await session.exec(select(SiteConfig).order_by(SiteConfig.key))).all())


async def set_value(
    session: AsyncSession,
    key: str,
    value: str,
    *,
    admin_user_id: Optional[int],
) -> SiteConfig:
    row = await _fetch(session, key)
    row.value = value
    row.updated_at = datetime.now(timezone.utc)
    row.updated_by = admin_user_id
    session.add(row)
    await session.commit()
    await session.refresh(row)
    return row
```

- [ ] **Step 4: 运行测试确认通过**

```bash
pytest tests/test_site_config.py -x -v
```
Expected: 5 passed

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/site_config.py backend/tests/test_site_config.py
git commit -m "feat(backend): site_config 读写服务 + 5 单测"
```

---

## Task 5: loan_service.accrue_interest（TDD）

**Files:**
- Create: `backend/tests/test_loan_service.py`
- Create: `backend/app/services/loan_service.py`

- [ ] **Step 1: 写失败测试**

`backend/tests/test_loan_service.py`：
```python
import sys, os
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

import pytest, pytest_asyncio, uuid
from decimal import Decimal
from datetime import datetime, timezone, timedelta
from sqlmodel import SQLModel
from app.core.database import engine, async_session_maker
from app.models.base import User
from app.services.loan_service import accrue_interest


@pytest_asyncio.fixture(autouse=True)
async def setup_db():
    async with engine.begin() as conn:
        await conn.run_sync(SQLModel.metadata.create_all)


def _new_user_sync(cash=Decimal("100"), debt=Decimal("0"), last_accrued=None):
    return User(
        username=f"u_{uuid.uuid4().hex[:6]}",
        email=f"{uuid.uuid4().hex[:6]}@t.com",
        casdoor_id=uuid.uuid4().hex,
        cash=cash,
        debt=debt,
        debt_last_accrued_at=last_accrued,
    )


def test_accrue_noop_when_debt_zero():
    u = _new_user_sync(debt=Decimal("0"), last_accrued=datetime.now(timezone.utc) - timedelta(hours=1))
    before = u.debt
    accrue_interest(u, Decimal("0.01"), datetime.now(timezone.utc))
    assert u.debt == before


def test_accrue_noop_when_last_accrued_none():
    u = _new_user_sync(debt=Decimal("100"), last_accrued=None)
    before = u.debt
    accrue_interest(u, Decimal("0.01"), datetime.now(timezone.utc))
    assert u.debt == before


def test_accrue_one_full_day_at_1pct():
    now = datetime.now(timezone.utc)
    u = _new_user_sync(debt=Decimal("1000"), last_accrued=now - timedelta(days=1))
    accrue_interest(u, Decimal("0.01"), now)
    # 每秒复利线性近似：debt * (1 + 0.01 * 86400/86400) = debt * 1.01
    assert abs(u.debt - Decimal("1010")) < Decimal("0.01")
    assert u.debt_last_accrued_at == now


def test_accrue_compounds_across_sweeps():
    """10 次连续 sweep，每次 60s，每次作用在当前 debt 上（复利）。"""
    now = datetime.now(timezone.utc)
    u = _new_user_sync(debt=Decimal("1000"), last_accrued=now)
    rate = Decimal("0.01")
    for i in range(10):
        now = now + timedelta(seconds=60)
        accrue_interest(u, rate, now)
    expected = Decimal("1000") * (Decimal(1) + rate * Decimal(60) / Decimal(86400)) ** 10
    assert abs(u.debt - expected) < Decimal("0.0001")


def test_accrue_negative_elapsed_noop():
    """时钟倒退不应倒扣利息（安全兜底）。"""
    now = datetime.now(timezone.utc)
    u = _new_user_sync(debt=Decimal("1000"), last_accrued=now + timedelta(hours=1))
    before = u.debt
    accrue_interest(u, Decimal("0.01"), now)
    assert u.debt == before
```

- [ ] **Step 2: 运行测试确认失败**

```bash
pytest tests/test_loan_service.py -x -v
```
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: 写实现**

`backend/app/services/loan_service.py`：
```python
"""Loan 业务原子操作。调用方负责事务边界。"""
from __future__ import annotations
from decimal import Decimal
from datetime import datetime

from app.models.base import User


_QUANT = Decimal("0.000001")


def accrue_interest(user: User, daily_rate: Decimal, now: datetime) -> None:
    """把从 user.debt_last_accrued_at 到 now 的利息折进 user.debt。
    复利：每次调用作用在当前 debt 上，增量 = debt * rate * elapsed_sec / 86400。
    debt==0 / last_accrued_at is None / elapsed<=0 时是 no-op。
    """
    if user.debt <= 0 or user.debt_last_accrued_at is None:
        return
    elapsed_sec = (now - user.debt_last_accrued_at).total_seconds()
    if elapsed_sec <= 0:
        return
    factor = Decimal(1) + daily_rate * Decimal(elapsed_sec) / Decimal(86400)
    user.debt = (user.debt * factor).quantize(_QUANT)
    user.debt_last_accrued_at = now
```

- [ ] **Step 4: 运行测试确认通过**

```bash
pytest tests/test_loan_service.py -x -v
```
Expected: 5 passed

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/loan_service.py backend/tests/test_loan_service.py
git commit -m "feat(backend): loan_service.accrue_interest + 5 单测（复利精度/漏跑补齐/时钟倒退兜底）"
```

---

## Task 6: loan_service.increase_debt / decrease_debt（TDD）

**Files:**
- Modify: `backend/tests/test_loan_service.py`
- Modify: `backend/app/services/loan_service.py`

- [ ] **Step 1: 追加失败测试**

在 `test_loan_service.py` 末尾追加：
```python
from app.services.loan_service import increase_debt, decrease_debt, compute_max_borrow


async def _create_user_in_db(**kwargs) -> int:
    async with async_session_maker() as s:
        async with s.begin():
            u = _new_user_sync(**kwargs)
            s.add(u)
            await s.flush()
            return u.id


@pytest.mark.asyncio
async def test_increase_debt_grant_cash_true():
    uid = await _create_user_in_db(cash=Decimal("100"), debt=Decimal("0"))
    async with async_session_maker() as s:
        u = await increase_debt(s, uid, Decimal("50"), grant_cash=True, daily_rate=Decimal("0.01"))
        await s.commit()
    async with async_session_maker() as s:
        u2 = await s.get(User, uid)
    assert u2.cash == Decimal("150.000000")
    assert u2.debt == Decimal("50.000000")
    assert u2.debt_last_accrued_at is not None


@pytest.mark.asyncio
async def test_increase_debt_accrues_existing_debt():
    """已有债务时先 accrue 再加 amount。"""
    now = datetime.now(timezone.utc)
    uid = await _create_user_in_db(
        cash=Decimal("0"),
        debt=Decimal("1000"),
        last_accrued=now - timedelta(days=1),
    )
    async with async_session_maker() as s:
        await increase_debt(s, uid, Decimal("100"), grant_cash=True, daily_rate=Decimal("0.01"))
        await s.commit()
    async with async_session_maker() as s:
        u = await s.get(User, uid)
    # 1000 * 1.01 + 100 ≈ 1110
    assert abs(u.debt - Decimal("1110")) < Decimal("0.1")
    assert u.cash == Decimal("100.000000")


@pytest.mark.asyncio
async def test_decrease_debt_consume_cash_true_partial():
    uid = await _create_user_in_db(cash=Decimal("200"), debt=Decimal("100"), last_accrued=datetime.now(timezone.utc))
    async with async_session_maker() as s:
        u, eff = await decrease_debt(s, uid, Decimal("30"), consume_cash=True, daily_rate=Decimal("0.01"))
        await s.commit()
    assert eff == Decimal("30")
    async with async_session_maker() as s:
        u2 = await s.get(User, uid)
    assert u2.cash == Decimal("170.000000")
    assert u2.debt == Decimal("70.000000")
    assert u2.debt_last_accrued_at is not None  # 仍有 debt


@pytest.mark.asyncio
async def test_decrease_debt_overpay_clamps_and_clears_timestamp():
    uid = await _create_user_in_db(cash=Decimal("1000"), debt=Decimal("100"), last_accrued=datetime.now(timezone.utc))
    async with async_session_maker() as s:
        u, eff = await decrease_debt(s, uid, Decimal("9999"), consume_cash=True, daily_rate=Decimal("0.01"))
        await s.commit()
    assert eff == Decimal("100.000000")  # 只扣到 debt
    async with async_session_maker() as s:
        u2 = await s.get(User, uid)
    assert u2.debt == Decimal("0.000000")
    assert u2.cash == Decimal("900.000000")
    assert u2.debt_last_accrued_at is None


@pytest.mark.asyncio
async def test_decrease_debt_forgive_no_cash_change():
    uid = await _create_user_in_db(cash=Decimal("200"), debt=Decimal("100"), last_accrued=datetime.now(timezone.utc))
    async with async_session_maker() as s:
        u, eff = await decrease_debt(s, uid, Decimal("40"), consume_cash=False, daily_rate=Decimal("0.01"))
        await s.commit()
    async with async_session_maker() as s:
        u2 = await s.get(User, uid)
    assert u2.cash == Decimal("200.000000")
    assert u2.debt == Decimal("60.000000")


def test_compute_max_borrow_basic():
    u = _new_user_sync(cash=Decimal("100"), debt=Decimal("0"))
    # net_worth = 100 + 50 = 150, k=1 → max=150
    assert compute_max_borrow(u, holdings_value=Decimal("50"), k=Decimal("1")) == Decimal("150")


def test_compute_max_borrow_with_existing_debt():
    u = _new_user_sync(cash=Decimal("100"), debt=Decimal("80"))
    # net_worth = 100 - 80 + 0 = 20, k=2 → k*nw=40, max=40-80=<0→0
    assert compute_max_borrow(u, holdings_value=Decimal("0"), k=Decimal("2")) == Decimal("0")


def test_compute_max_borrow_positive_headroom():
    u = _new_user_sync(cash=Decimal("500"), debt=Decimal("100"))
    # net_worth = 500-100+200 = 600, k=1 → 600-100=500
    assert compute_max_borrow(u, holdings_value=Decimal("200"), k=Decimal("1")) == Decimal("500")
```

- [ ] **Step 2: 运行测试确认失败**

```bash
pytest tests/test_loan_service.py -x -v
```
Expected: 新加的测试 FAIL（`ImportError` 或 `AttributeError`）

- [ ] **Step 3: 追加实现**

在 `app/services/loan_service.py` 末尾追加：
```python
from sqlalchemy.ext.asyncio import AsyncSession
from sqlmodel import select
from datetime import datetime, timezone


class LoanServiceError(Exception):
    pass


def _now_utc() -> datetime:
    return datetime.now(timezone.utc)


async def increase_debt(
    session: AsyncSession,
    user_id: int,
    amount: Decimal,
    *,
    grant_cash: bool,
    daily_rate: Decimal,
) -> User:
    """SELECT FOR UPDATE user → accrue → debt += amount；grant_cash=True 时 cash += amount。
    调用方负责 commit。amount 必须 > 0，否则 ValueError。
    """
    if amount <= 0:
        raise ValueError("amount must be positive")
    stmt = select(User).where(User.id == user_id).with_for_update()
    u = (await session.exec(stmt)).one()
    now = _now_utc()
    accrue_interest(u, daily_rate, now)
    u.debt = (u.debt + amount).quantize(_QUANT)
    if u.debt_last_accrued_at is None:
        u.debt_last_accrued_at = now
    if grant_cash:
        u.cash = (u.cash + amount).quantize(_QUANT)
    session.add(u)
    return u


async def decrease_debt(
    session: AsyncSession,
    user_id: int,
    amount: Decimal,
    *,
    consume_cash: bool,
    daily_rate: Decimal,
) -> tuple[User, Decimal]:
    """SELECT FOR UPDATE user → accrue → effective = min(amount, debt) → debt -= effective。
    consume_cash=True 时 cash -= effective；False 时 cash 不变。
    debt 归零时清 last_accrued_at。调用方负责 commit。
    返回 (user, effective_amount)。amount 必须 > 0。
    """
    if amount <= 0:
        raise ValueError("amount must be positive")
    stmt = select(User).where(User.id == user_id).with_for_update()
    u = (await session.exec(stmt)).one()
    now = _now_utc()
    accrue_interest(u, daily_rate, now)
    effective = min(amount, u.debt).quantize(_QUANT)
    if effective <= 0:
        return u, Decimal("0")
    u.debt = (u.debt - effective).quantize(_QUANT)
    if consume_cash:
        u.cash = (u.cash - effective).quantize(_QUANT)
    if u.debt <= 0:
        u.debt = Decimal("0")
        u.debt_last_accrued_at = None
    session.add(u)
    return u, effective


def compute_max_borrow(user: User, holdings_value: Decimal, k: Decimal) -> Decimal:
    """max(0, k × (cash - debt + holdings_value) - debt)"""
    net_worth = user.cash - user.debt + holdings_value
    headroom = (k * net_worth - user.debt)
    return max(Decimal("0"), headroom).quantize(_QUANT)
```

- [ ] **Step 4: 运行测试确认通过**

```bash
pytest tests/test_loan_service.py -x -v
```
Expected: 12 passed

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/loan_service.py backend/tests/test_loan_service.py
git commit -m "feat(backend): loan_service 原子操作 increase_debt/decrease_debt/compute_max_borrow"
```

---

## Task 7: loan_sweep 模块（TDD）

**Files:**
- Create: `backend/tests/test_loan_sweep.py`
- Create: `backend/app/services/loan_sweep.py`

- [ ] **Step 1: 写失败测试**

`backend/tests/test_loan_sweep.py`：
```python
import sys, os
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

import pytest, pytest_asyncio, uuid
from decimal import Decimal
from datetime import datetime, timezone, timedelta
from sqlmodel import SQLModel
from app.core.database import engine, async_session_maker
from app.models.base import User, SiteConfig
from app.services.loan_sweep import run_sweep_once


@pytest_asyncio.fixture(autouse=True)
async def setup_db():
    async with engine.begin() as conn:
        await conn.run_sync(SQLModel.metadata.create_all)
    async with async_session_maker() as s:
        async with s.begin():
            s.add(SiteConfig(key="loan_daily_rate", value="0.01", value_type="decimal"))


async def _seed_user(debt, last_accrued):
    async with async_session_maker() as s:
        async with s.begin():
            u = User(
                username=f"u_{uuid.uuid4().hex[:6]}",
                email=f"{uuid.uuid4().hex[:6]}@t.com",
                casdoor_id=uuid.uuid4().hex,
                cash=Decimal("0"),
                debt=Decimal(debt),
                debt_last_accrued_at=last_accrued,
            )
            s.add(u)
            await s.flush()
            return u.id


@pytest.mark.asyncio
async def test_sweep_skips_users_with_zero_debt():
    uid = await _seed_user(debt="0", last_accrued=None)
    await run_sweep_once()
    async with async_session_maker() as s:
        u = await s.get(User, uid)
    assert u.debt == Decimal("0")


@pytest.mark.asyncio
async def test_sweep_accrues_interest():
    now = datetime.now(timezone.utc)
    uid = await _seed_user(debt="1000", last_accrued=now - timedelta(hours=1))
    await run_sweep_once()
    async with async_session_maker() as s:
        u = await s.get(User, uid)
    # 1h @ 1%/day ≈ 1000 * (1 + 0.01/24) ≈ 1000.4167
    assert u.debt > Decimal("1000.3")
    assert u.debt < Decimal("1000.5")
    assert u.debt_last_accrued_at is not None
    assert u.debt_last_accrued_at > now - timedelta(seconds=5)


@pytest.mark.asyncio
async def test_sweep_multiple_users_independent():
    now = datetime.now(timezone.utc)
    uid1 = await _seed_user(debt="100", last_accrued=now - timedelta(days=1))
    uid2 = await _seed_user(debt="0", last_accrued=None)
    await run_sweep_once()
    async with async_session_maker() as s:
        u1 = await s.get(User, uid1)
        u2 = await s.get(User, uid2)
    assert u1.debt > Decimal("100.5") and u1.debt < Decimal("101.5")
    assert u2.debt == Decimal("0")
```

- [ ] **Step 2: 运行测试确认失败**

```bash
pytest tests/test_loan_sweep.py -x -v
```
Expected: `ModuleNotFoundError: app.services.loan_sweep`

- [ ] **Step 3: 写实现**

`backend/app/services/loan_sweep.py`：
```python
"""APS 定时结息 sweep。
- run_sweep_once()：扫一次全体欠债用户
- start_scheduler(app) / stop_scheduler()：FastAPI lifespan 里调
- reschedule(interval_sec)：管理员改间隔后调用
"""
from __future__ import annotations
import logging
from datetime import datetime, timezone
from decimal import Decimal

from apscheduler.schedulers.asyncio import AsyncIOScheduler
from sqlalchemy.ext.asyncio import AsyncSession
from sqlmodel import select

from app.core.database import async_session_maker
from app.models.base import User
from app.services.loan_service import accrue_interest
from app.services import site_config


logger = logging.getLogger("thccb.loan_sweep")

_scheduler: AsyncIOScheduler | None = None
_JOB_ID = "loan_sweep_tick"


async def run_sweep_once() -> int:
    """扫一次全体 debt>0 用户，accrue 并 commit。返回结息户数。"""
    async with async_session_maker() as session:
        rate = await site_config.get_decimal(session, "loan_daily_rate")
    if rate <= 0:
        logger.debug("sweep skip: rate<=0")
        return 0

    touched = 0
    async with async_session_maker() as session:
        ids = list((await session.exec(select(User.id).where(User.debt > 0))).all())

    for uid in ids:
        async with async_session_maker() as session:
            async with session.begin():
                u = (await session.exec(
                    select(User).where(User.id == uid).with_for_update()
                )).one()
                before = u.debt
                accrue_interest(u, rate, datetime.now(timezone.utc))
                if u.debt != before:
                    session.add(u)
                    touched += 1
    if touched:
        logger.info("sweep tick: touched=%s", touched)
    return touched


async def _tick_safe():
    try:
        await run_sweep_once()
    except Exception:
        logger.exception("loan sweep tick failed")


async def start_scheduler() -> None:
    global _scheduler
    if _scheduler is not None:
        return
    async with async_session_maker() as session:
        interval = await site_config.get_int(session, "loan_sweep_interval_sec")
    interval = max(10, min(3600, interval))
    _scheduler = AsyncIOScheduler(timezone="UTC")
    _scheduler.add_job(_tick_safe, "interval", seconds=interval, id=_JOB_ID, max_instances=1)
    _scheduler.start()
    logger.info("loan sweep started interval=%ss", interval)


async def stop_scheduler() -> None:
    global _scheduler
    if _scheduler is not None:
        _scheduler.shutdown(wait=False)
        _scheduler = None


async def reschedule(interval_sec: int) -> None:
    global _scheduler
    if _scheduler is None:
        return
    interval = max(10, min(3600, interval_sec))
    _scheduler.reschedule_job(_JOB_ID, trigger="interval", seconds=interval)
    logger.info("loan sweep rescheduled interval=%ss", interval)
```

- [ ] **Step 4: 运行测试确认通过**

```bash
pytest tests/test_loan_sweep.py -x -v
```
Expected: 3 passed

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/loan_sweep.py backend/tests/test_loan_sweep.py
git commit -m "feat(backend): loan_sweep — APS 定时结息 + 3 单测"
```

---

## Task 8: lifespan 注册 sweep

**Files:**
- Modify: `backend/app/main.py`

- [ ] **Step 1: import + lifespan 挂钩**

打开 `backend/app/main.py`，顶部 import 区追加：
```python
from app.services.loan_sweep import start_scheduler, stop_scheduler
```

把 `lifespan` 函数改成：
```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    await init_db()
    setup_admin(app, engine)
    await start_scheduler()
    yield
    await stop_scheduler()
    await engine.dispose()
```

- [ ] **Step 2: 启动自检**

```bash
cd backend && source venv/bin/activate && python -c "import app.main; print('ok')"
```
Expected: 输出 `ok`（无 import 错）

- [ ] **Step 3: Commit**

```bash
git add backend/app/main.py
git commit -m "feat(backend): lifespan 启动/停止 loan_sweep scheduler"
```

---

## Task 9: 玩家 Loan API schema

**Files:**
- Create: `backend/app/schemas/loan.py`

- [ ] **Step 1: 写 schema**

```python
"""Loan API 请求/响应模型。"""
from decimal import Decimal
from typing import Optional
from datetime import datetime
from pydantic import BaseModel, Field, condecimal


class BorrowRequest(BaseModel):
    amount: condecimal(gt=0, max_digits=16, decimal_places=6)


class RepayRequest(BaseModel):
    amount: condecimal(gt=0, max_digits=16, decimal_places=6)


class LoanQuotaResponse(BaseModel):
    enabled: bool
    cash: Decimal
    debt: Decimal
    net_worth: Decimal
    leverage_k: Decimal
    daily_rate: Decimal
    max_borrow: Decimal
    last_accrued_at: Optional[datetime]


class LoanActionResponse(BaseModel):
    cash: Decimal
    debt: Decimal
    max_borrow: Decimal


class ForceLoanRequest(BaseModel):
    amount: condecimal(gt=0, max_digits=16, decimal_places=6)
    reason: str = Field(..., min_length=1, max_length=200)


class ForgiveDebtRequest(BaseModel):
    amount: condecimal(gt=0, max_digits=16, decimal_places=6)
    reason: str = Field(..., min_length=1, max_length=200)


class SiteConfigItem(BaseModel):
    key: str
    value: str
    value_type: str
    updated_at: datetime
    updated_by: Optional[int]


class SiteConfigUpdate(BaseModel):
    value: str = Field(..., min_length=1, max_length=200)
```

- [ ] **Step 2: 语法检查**

```bash
cd backend && python -m py_compile app/schemas/loan.py
```
Expected: 无输出

- [ ] **Step 3: Commit**

```bash
git add backend/app/schemas/loan.py
git commit -m "feat(backend): loan 模块请求/响应 schema"
```

---

## Task 10: 玩家 /loan/quota（TDD）

**Files:**
- Create: `backend/tests/test_loan_api.py`
- Create: `backend/app/api/v1/loan.py`
- Modify: `backend/app/main.py`

- [ ] **Step 1: 写失败测试**

`backend/tests/test_loan_api.py`（沿用 test_auth.py 的 fixture 模式）：
```python
import sys, os
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

import pytest, pytest_asyncio, uuid
from decimal import Decimal
from httpx import AsyncClient
from asgi_lifespan import LifespanManager
from sqlmodel import SQLModel
from app.main import app
from app.core.database import engine, async_session_maker
from app.core.users import create_access_token
from app.models.base import User, SiteConfig


@pytest_asyncio.fixture(autouse=True)
async def setup_db():
    async with engine.begin() as conn:
        await conn.run_sync(SQLModel.metadata.create_all)
    async with async_session_maker() as s:
        async with s.begin():
            s.add(SiteConfig(key="loan_enabled", value="true", value_type="bool"))
            s.add(SiteConfig(key="loan_leverage_k", value="1.0", value_type="decimal"))
            s.add(SiteConfig(key="loan_daily_rate", value="0.01", value_type="decimal"))
            s.add(SiteConfig(key="loan_sweep_interval_sec", value="60", value_type="int"))


@pytest_asyncio.fixture
async def client():
    async with LifespanManager(app):
        async with AsyncClient(base_url="http://localhost:8004", app=app) as ac:
            yield ac


async def _make_user(cash=Decimal("1000"), debt=Decimal("0"), superuser=False):
    suffix = uuid.uuid4().hex[:6]
    async with async_session_maker() as s:
        async with s.begin():
            u = User(
                username=f"u_{suffix}",
                email=f"{suffix}@t.com",
                casdoor_id=f"cd_{suffix}",
                cash=cash,
                debt=debt,
                is_superuser=superuser,
            )
            s.add(u)
            await s.flush()
            uid = u.id
    token = create_access_token({"sub": str(uid)})
    return uid, {"Authorization": f"Bearer {token}"}


@pytest.mark.asyncio
async def test_quota_returns_fields(client):
    _, h = await _make_user(cash=Decimal("500"))
    r = await client.get("/api/v1/loan/quota", headers=h)
    assert r.status_code == 200
    body = r.json()
    assert body["enabled"] is True
    assert Decimal(body["cash"]) == Decimal("500")
    assert Decimal(body["debt"]) == Decimal("0")
    assert Decimal(body["max_borrow"]) >= Decimal("500")
    assert Decimal(body["daily_rate"]) == Decimal("0.01")
```

- [ ] **Step 2: 运行测试确认失败**

```bash
pytest tests/test_loan_api.py -x -v
```
Expected: 404（路由不存在）

- [ ] **Step 3: 写 loan.py router**

`backend/app/api/v1/loan.py`：
```python
"""Loan 玩家接口。所有 handler 为 loan_service 薄封装。"""
from __future__ import annotations
import logging
from decimal import Decimal
from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.ext.asyncio import AsyncSession
from sqlmodel import select

from app.core.database import get_async_session
from app.core.users import current_active_user
from app.models.base import User, Position, Outcome, Market, MarketStatus
from app.schemas.loan import (
    BorrowRequest, RepayRequest, LoanQuotaResponse, LoanActionResponse,
)
from app.services import site_config, loan_service
from app.services.lmsr import market_price

router = APIRouter()
logger = logging.getLogger("thccb.loan")


async def _holdings_value(db: AsyncSession, user_id: int) -> Decimal:
    """用 LMSR 瞬时价估算（与 /user/me 同源语义，简化版；清算价值留给 portfolio）。"""
    positions = list((await db.exec(select(Position).where(Position.user_id == user_id))).all())
    total = Decimal("0")
    for p in positions:
        if p.amount <= 0:
            continue
        o = await db.get(Outcome, p.outcome_id)
        if not o:
            continue
        m = await db.get(Market, o.market_id)
        if not m or m.status != MarketStatus.TRADING:
            continue
        outs = list((await db.exec(select(Outcome).where(Outcome.market_id == m.id))).all())
        idx = next((i for i, x in enumerate(outs) if x.id == o.id), None)
        if idx is None:
            continue
        shares = [float(x.total_shares) for x in outs]
        price = Decimal(str(market_price(shares, idx, m.liquidity_b)))
        total += (p.amount * price).quantize(Decimal("0.000001"))
    return total


async def _build_quota(db: AsyncSession, user: User) -> LoanQuotaResponse:
    enabled = await site_config.get_bool(db, "loan_enabled")
    k = await site_config.get_decimal(db, "loan_leverage_k")
    rate = await site_config.get_decimal(db, "loan_daily_rate")
    hv = await _holdings_value(db, user.id)
    net_worth = (user.cash - user.debt + hv).quantize(Decimal("0.000001"))
    max_borrow = loan_service.compute_max_borrow(user, hv, k)
    return LoanQuotaResponse(
        enabled=enabled,
        cash=user.cash,
        debt=user.debt,
        net_worth=net_worth,
        leverage_k=k,
        daily_rate=rate,
        max_borrow=max_borrow,
        last_accrued_at=user.debt_last_accrued_at,
    )


@router.get("/quota", response_model=LoanQuotaResponse)
async def get_quota(
    user: User = Depends(current_active_user),
    db: AsyncSession = Depends(get_async_session),
):
    return await _build_quota(db, user)
```

把 router 挂进 `backend/app/main.py` — 找到 `app.include_router(stream.router, ...)` 那一行，其后追加：
```python
from app.api.v1 import loan
app.include_router(loan.router, prefix="/api/v1/loan", tags=["Loan"])
```

并把顶部 `from app.api.v1 import auth, user, market, chart, stream` 改为 `from app.api.v1 import auth, user, market, chart, stream, loan`。

- [ ] **Step 4: 运行测试确认通过**

```bash
pytest tests/test_loan_api.py -x -v
```
Expected: 1 passed

- [ ] **Step 5: Commit**

```bash
git add backend/app/api/v1/loan.py backend/app/main.py backend/tests/test_loan_api.py
git commit -m "feat(backend): GET /loan/quota + 1 单测"
```

---

## Task 11: 玩家 /loan/borrow（TDD）

**Files:**
- Modify: `backend/tests/test_loan_api.py`
- Modify: `backend/app/api/v1/loan.py`

- [ ] **Step 1: 追加失败测试**

追加到 `test_loan_api.py`：
```python
@pytest.mark.asyncio
async def test_borrow_success_updates_cash_and_debt(client):
    uid, h = await _make_user(cash=Decimal("1000"))
    r = await client.post("/api/v1/loan/borrow", json={"amount": "500"}, headers=h)
    assert r.status_code == 200, r.text
    async with async_session_maker() as s:
        u = await s.get(User, uid)
    assert u.cash == Decimal("1500.000000")
    assert u.debt == Decimal("500.000000")
    assert u.debt_last_accrued_at is not None


@pytest.mark.asyncio
async def test_borrow_exceeds_quota_400(client):
    _, h = await _make_user(cash=Decimal("100"))
    # k=1, net_worth=100, max_borrow=100；借 200 应拒
    r = await client.post("/api/v1/loan/borrow", json={"amount": "200"}, headers=h)
    assert r.status_code == 400


@pytest.mark.asyncio
async def test_borrow_when_disabled_403(client):
    async with async_session_maker() as s:
        row = (await s.exec(select(SiteConfig).where(SiteConfig.key == "loan_enabled"))).one()
        row.value = "false"
        s.add(row)
        await s.commit()
    _, h = await _make_user(cash=Decimal("1000"))
    r = await client.post("/api/v1/loan/borrow", json={"amount": "100"}, headers=h)
    assert r.status_code == 403


@pytest.mark.asyncio
async def test_borrow_nonpositive_422(client):
    _, h = await _make_user(cash=Decimal("1000"))
    r = await client.post("/api/v1/loan/borrow", json={"amount": "0"}, headers=h)
    assert r.status_code == 422  # pydantic gt=0
```

别忘了在 test 文件顶部追加 `from sqlmodel import select`（若未 import）。

- [ ] **Step 2: 运行测试确认失败**

```bash
pytest tests/test_loan_api.py -x -v
```
Expected: 新测试 FAIL（404/405）

- [ ] **Step 3: 追加 borrow handler**

在 `app/api/v1/loan.py` 末尾追加：
```python
@router.post("/borrow", response_model=LoanActionResponse)
async def borrow(
    req: BorrowRequest,
    user: User = Depends(current_active_user),
    db: AsyncSession = Depends(get_async_session),
):
    enabled = await site_config.get_bool(db, "loan_enabled")
    if not enabled:
        raise HTTPException(status_code=403, detail="借款功能已关闭")

    k = await site_config.get_decimal(db, "loan_leverage_k")
    rate = await site_config.get_decimal(db, "loan_daily_rate")
    amount = Decimal(req.amount)

    # quota 校验（读用 User 的快照；锁内再校验一次防并发）
    hv = await _holdings_value(db, user.id)
    max_borrow = loan_service.compute_max_borrow(user, hv, k)
    if amount > max_borrow:
        raise HTTPException(
            status_code=400,
            detail=f"借款额超出额度（可借 {max_borrow}，申请 {amount}）",
        )

    u = await loan_service.increase_debt(
        db, user.id, amount, grant_cash=True, daily_rate=rate,
    )
    await db.commit()
    await db.refresh(u)
    logger.info(
        "LOAN_BORROW user_id=%s amount=%s new_cash=%s new_debt=%s",
        user.id, amount, u.cash, u.debt,
    )
    hv2 = await _holdings_value(db, u.id)
    return LoanActionResponse(
        cash=u.cash,
        debt=u.debt,
        max_borrow=loan_service.compute_max_borrow(u, hv2, k),
    )
```

- [ ] **Step 4: 运行测试确认通过**

```bash
pytest tests/test_loan_api.py -x -v
```
Expected: 5 passed

- [ ] **Step 5: Commit**

```bash
git add backend/app/api/v1/loan.py backend/tests/test_loan_api.py
git commit -m "feat(backend): POST /loan/borrow + 4 单测（成功/超额/禁用/非正）"
```

---

## Task 12: 玩家 /loan/repay（TDD）

**Files:**
- Modify: `backend/tests/test_loan_api.py`
- Modify: `backend/app/api/v1/loan.py`

- [ ] **Step 1: 追加失败测试**

```python
@pytest.mark.asyncio
async def test_repay_partial(client):
    uid, h = await _make_user(cash=Decimal("1000"), debt=Decimal("200"))
    async with async_session_maker() as s:
        u = await s.get(User, uid)
        from datetime import datetime, timezone
        u.debt_last_accrued_at = datetime.now(timezone.utc)
        await s.commit()
    r = await client.post("/api/v1/loan/repay", json={"amount": "50"}, headers=h)
    assert r.status_code == 200
    async with async_session_maker() as s:
        u2 = await s.get(User, uid)
    assert u2.cash == Decimal("950.000000")
    assert u2.debt == Decimal("150.000000")


@pytest.mark.asyncio
async def test_repay_overpay_clamps_to_debt(client):
    uid, h = await _make_user(cash=Decimal("1000"), debt=Decimal("50"))
    async with async_session_maker() as s:
        u = await s.get(User, uid)
        from datetime import datetime, timezone
        u.debt_last_accrued_at = datetime.now(timezone.utc)
        await s.commit()
    r = await client.post("/api/v1/loan/repay", json={"amount": "9999"}, headers=h)
    assert r.status_code == 200
    async with async_session_maker() as s:
        u2 = await s.get(User, uid)
    assert u2.debt == Decimal("0.000000")
    assert u2.cash == Decimal("950.000000")
    assert u2.debt_last_accrued_at is None


@pytest.mark.asyncio
async def test_repay_exceeds_cash_400(client):
    _, h = await _make_user(cash=Decimal("30"), debt=Decimal("200"))
    r = await client.post("/api/v1/loan/repay", json={"amount": "100"}, headers=h)
    assert r.status_code == 400
```

- [ ] **Step 2: 运行测试确认失败**

```bash
pytest tests/test_loan_api.py -x -v
```

- [ ] **Step 3: 追加 repay handler**

```python
@router.post("/repay", response_model=LoanActionResponse)
async def repay(
    req: RepayRequest,
    user: User = Depends(current_active_user),
    db: AsyncSession = Depends(get_async_session),
):
    rate = await site_config.get_decimal(db, "loan_daily_rate")
    k = await site_config.get_decimal(db, "loan_leverage_k")
    amount = Decimal(req.amount)
    if amount > user.cash:
        raise HTTPException(status_code=400, detail="还款额超过现金余额")

    u, effective = await loan_service.decrease_debt(
        db, user.id, amount, consume_cash=True, daily_rate=rate,
    )
    await db.commit()
    await db.refresh(u)
    logger.info(
        "LOAN_REPAY user_id=%s requested=%s effective=%s new_cash=%s new_debt=%s",
        user.id, amount, effective, u.cash, u.debt,
    )
    hv = await _holdings_value(db, u.id)
    return LoanActionResponse(
        cash=u.cash,
        debt=u.debt,
        max_borrow=loan_service.compute_max_borrow(u, hv, k),
    )
```

- [ ] **Step 4: 运行测试确认通过**

```bash
pytest tests/test_loan_api.py -x -v
```
Expected: 8 passed

- [ ] **Step 5: Commit**

```bash
git add backend/app/api/v1/loan.py backend/tests/test_loan_api.py
git commit -m "feat(backend): POST /loan/repay + 3 单测"
```

---

## Task 13: 管理员 SiteConfig CRUD（TDD）

**Files:**
- Create: `backend/app/api/v1/site_config.py`
- Create: `backend/tests/test_site_config_api.py`
- Modify: `backend/app/main.py`

- [ ] **Step 1: 写失败测试**

`backend/tests/test_site_config_api.py`：
```python
import sys, os
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))
import pytest, pytest_asyncio, uuid
from decimal import Decimal
from httpx import AsyncClient
from asgi_lifespan import LifespanManager
from sqlmodel import SQLModel
from app.main import app
from app.core.database import engine, async_session_maker
from app.core.users import create_access_token
from app.models.base import User, SiteConfig


@pytest_asyncio.fixture(autouse=True)
async def setup_db():
    async with engine.begin() as conn:
        await conn.run_sync(SQLModel.metadata.create_all)
    async with async_session_maker() as s:
        async with s.begin():
            s.add(SiteConfig(key="loan_daily_rate", value="0.01", value_type="decimal"))
            s.add(SiteConfig(key="loan_sweep_interval_sec", value="60", value_type="int"))
            s.add(SiteConfig(key="loan_enabled", value="true", value_type="bool"))
            s.add(SiteConfig(key="loan_leverage_k", value="1.0", value_type="decimal"))


@pytest_asyncio.fixture
async def client():
    async with LifespanManager(app):
        async with AsyncClient(base_url="http://localhost:8004", app=app) as ac:
            yield ac


async def _make_user(superuser=False):
    suffix = uuid.uuid4().hex[:6]
    async with async_session_maker() as s:
        async with s.begin():
            u = User(username=f"u_{suffix}", email=f"{suffix}@t.com",
                    casdoor_id=f"cd_{suffix}", cash=Decimal("0"), is_superuser=superuser)
            s.add(u)
            await s.flush()
            uid = u.id
    token = create_access_token({"sub": str(uid)})
    return uid, {"Authorization": f"Bearer {token}"}


@pytest.mark.asyncio
async def test_list_requires_superuser(client):
    _, h = await _make_user(superuser=False)
    r = await client.get("/api/v1/admin/site-config", headers=h)
    assert r.status_code in (401, 403)


@pytest.mark.asyncio
async def test_list_returns_all_keys(client):
    _, h = await _make_user(superuser=True)
    r = await client.get("/api/v1/admin/site-config", headers=h)
    assert r.status_code == 200
    keys = {item["key"] for item in r.json()}
    assert {"loan_enabled", "loan_leverage_k", "loan_daily_rate", "loan_sweep_interval_sec"} <= keys


@pytest.mark.asyncio
async def test_update_decimal_value(client):
    _, h = await _make_user(superuser=True)
    r = await client.put("/api/v1/admin/site-config/loan_daily_rate", json={"value": "0.05"}, headers=h)
    assert r.status_code == 200
    r2 = await client.get("/api/v1/admin/site-config", headers=h)
    rates = {i["key"]: i["value"] for i in r2.json()}
    assert rates["loan_daily_rate"] == "0.05"


@pytest.mark.asyncio
async def test_update_rejects_unknown_key(client):
    _, h = await _make_user(superuser=True)
    r = await client.put("/api/v1/admin/site-config/not_whitelisted", json={"value": "x"}, headers=h)
    assert r.status_code == 400


@pytest.mark.asyncio
async def test_update_rejects_invalid_decimal(client):
    _, h = await _make_user(superuser=True)
    r = await client.put("/api/v1/admin/site-config/loan_daily_rate", json={"value": "abc"}, headers=h)
    assert r.status_code == 400


@pytest.mark.asyncio
async def test_update_rejects_rate_out_of_range(client):
    _, h = await _make_user(superuser=True)
    r = await client.put("/api/v1/admin/site-config/loan_daily_rate", json={"value": "2.0"}, headers=h)
    assert r.status_code == 400  # 要求 rate ∈ (0, 1)
```

- [ ] **Step 2: 运行测试确认失败**

```bash
pytest tests/test_site_config_api.py -x -v
```

- [ ] **Step 3: 写实现**

`backend/app/api/v1/site_config.py`：
```python
"""超管站点配置接口。"""
from __future__ import annotations
import logging
from decimal import Decimal, InvalidOperation
from typing import List

from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.database import get_async_session
from app.core.users import current_superuser
from app.models.base import User
from app.schemas.loan import SiteConfigItem, SiteConfigUpdate
from app.services import site_config, loan_sweep

router = APIRouter()
logger = logging.getLogger("thccb.site_config")

_WHITELIST = {
    "loan_enabled": "bool",
    "loan_leverage_k": "decimal",
    "loan_daily_rate": "decimal",
    "loan_sweep_interval_sec": "int",
}


def _validate(key: str, value: str) -> None:
    t = _WHITELIST[key]
    if t == "bool":
        if value.lower() not in ("true", "false"):
            raise HTTPException(status_code=400, detail="bool 必须为 true/false")
    elif t == "int":
        try:
            v = int(value)
        except ValueError:
            raise HTTPException(status_code=400, detail="int 解析失败")
        if key == "loan_sweep_interval_sec" and not (10 <= v <= 3600):
            raise HTTPException(status_code=400, detail="sweep 间隔必须在 [10, 3600] 秒")
    elif t == "decimal":
        try:
            v = Decimal(value)
        except InvalidOperation:
            raise HTTPException(status_code=400, detail="decimal 解析失败")
        if key == "loan_daily_rate" and not (Decimal("0") < v < Decimal("1")):
            raise HTTPException(status_code=400, detail="日利率必须在 (0, 1)")
        if key == "loan_leverage_k" and not (Decimal("0") < v <= Decimal("10")):
            raise HTTPException(status_code=400, detail="杠杆倍数必须在 (0, 10]")


@router.get("/site-config", response_model=List[SiteConfigItem])
async def list_configs(
    admin: User = Depends(current_superuser),
    db: AsyncSession = Depends(get_async_session),
):
    rows = await site_config.get_all(db)
    return [SiteConfigItem(
        key=r.key, value=r.value, value_type=r.value_type,
        updated_at=r.updated_at, updated_by=r.updated_by,
    ) for r in rows]


@router.put("/site-config/{key}", response_model=SiteConfigItem)
async def update_config(
    key: str,
    req: SiteConfigUpdate,
    admin: User = Depends(current_superuser),
    db: AsyncSession = Depends(get_async_session),
):
    if key not in _WHITELIST:
        raise HTTPException(status_code=400, detail=f"未知配置 key：{key}")
    _validate(key, req.value)

    row = await site_config.set_value(db, key, req.value, admin_user_id=admin.id)
    logger.info("SITECONFIG_SET admin_id=%s key=%s value=%s", admin.id, key, req.value)

    if key == "loan_sweep_interval_sec":
        try:
            await loan_sweep.reschedule(int(req.value))
        except Exception:
            logger.exception("reschedule failed")

    return SiteConfigItem(
        key=row.key, value=row.value, value_type=row.value_type,
        updated_at=row.updated_at, updated_by=row.updated_by,
    )
```

在 `backend/app/main.py` import 区追加 `site_config as site_config_api`，并挂载：
```python
from app.api.v1 import site_config as site_config_api
app.include_router(site_config_api.router, prefix="/api/v1/admin", tags=["Admin"])
```

- [ ] **Step 4: 运行测试确认通过**

```bash
pytest tests/test_site_config_api.py -x -v
```
Expected: 6 passed

- [ ] **Step 5: Commit**

```bash
git add backend/app/api/v1/site_config.py backend/app/main.py backend/tests/test_site_config_api.py
git commit -m "feat(backend): 超管 /admin/site-config CRUD + 6 单测（含 sweep 重排程）"
```

---

## Task 14: 管理员 force-loan / forgive-debt（TDD）

**Files:**
- Modify: `backend/app/api/v1/user.py`
- Create: `backend/tests/test_loan_admin.py`

- [ ] **Step 1: 写失败测试**

`backend/tests/test_loan_admin.py`：
```python
import sys, os
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))
import pytest, pytest_asyncio, uuid
from decimal import Decimal
from httpx import AsyncClient
from asgi_lifespan import LifespanManager
from sqlmodel import SQLModel, select
from app.main import app
from app.core.database import engine, async_session_maker
from app.core.users import create_access_token
from app.models.base import User, SiteConfig


@pytest_asyncio.fixture(autouse=True)
async def setup_db():
    async with engine.begin() as conn:
        await conn.run_sync(SQLModel.metadata.create_all)
    async with async_session_maker() as s:
        async with s.begin():
            s.add(SiteConfig(key="loan_enabled", value="true", value_type="bool"))
            s.add(SiteConfig(key="loan_daily_rate", value="0.01", value_type="decimal"))
            s.add(SiteConfig(key="loan_leverage_k", value="1.0", value_type="decimal"))
            s.add(SiteConfig(key="loan_sweep_interval_sec", value="60", value_type="int"))


@pytest_asyncio.fixture
async def client():
    async with LifespanManager(app):
        async with AsyncClient(base_url="http://localhost:8004", app=app) as ac:
            yield ac


async def _make_user(cash=Decimal("0"), debt=Decimal("0"), superuser=False):
    suffix = uuid.uuid4().hex[:6]
    async with async_session_maker() as s:
        async with s.begin():
            u = User(username=f"u_{suffix}", email=f"{suffix}@t.com",
                    casdoor_id=f"cd_{suffix}", cash=cash, debt=debt, is_superuser=superuser)
            s.add(u)
            await s.flush()
            uid = u.id
    token = create_access_token({"sub": str(uid)})
    return uid, {"Authorization": f"Bearer {token}"}


@pytest.mark.asyncio
async def test_force_loan_requires_superuser(client):
    target_uid, _ = await _make_user()
    _, h = await _make_user(superuser=False)
    r = await client.post(f"/api/v1/user/{target_uid}/force-loan",
                          json={"amount": "100", "reason": "test"}, headers=h)
    assert r.status_code in (401, 403)


@pytest.mark.asyncio
async def test_force_loan_grants_cash_and_debt(client):
    target_uid, _ = await _make_user(cash=Decimal("50"))
    _, h = await _make_user(superuser=True)
    r = await client.post(f"/api/v1/user/{target_uid}/force-loan",
                          json={"amount": "500", "reason": "活动奖励"}, headers=h)
    assert r.status_code == 200, r.text
    async with async_session_maker() as s:
        u = await s.get(User, target_uid)
    assert u.cash == Decimal("550.000000")
    assert u.debt == Decimal("500.000000")


@pytest.mark.asyncio
async def test_force_loan_blocked_when_disabled(client):
    async with async_session_maker() as s:
        row = (await s.exec(select(SiteConfig).where(SiteConfig.key == "loan_enabled"))).one()
        row.value = "false"
        s.add(row)
        await s.commit()
    target_uid, _ = await _make_user()
    _, h = await _make_user(superuser=True)
    r = await client.post(f"/api/v1/user/{target_uid}/force-loan",
                          json={"amount": "100", "reason": "x"}, headers=h)
    assert r.status_code == 403


@pytest.mark.asyncio
async def test_forgive_debt_reduces_debt_no_cash_change(client):
    target_uid, _ = await _make_user(cash=Decimal("50"), debt=Decimal("200"))
    async with async_session_maker() as s:
        u = await s.get(User, target_uid)
        from datetime import datetime, timezone
        u.debt_last_accrued_at = datetime.now(timezone.utc)
        await s.commit()
    _, h = await _make_user(superuser=True)
    r = await client.post(f"/api/v1/user/{target_uid}/forgive-debt",
                          json={"amount": "80", "reason": "compensation"}, headers=h)
    assert r.status_code == 200
    async with async_session_maker() as s:
        u = await s.get(User, target_uid)
    assert u.cash == Decimal("50.000000")
    assert u.debt == Decimal("120.000000")


@pytest.mark.asyncio
async def test_forgive_overpay_clears_debt(client):
    target_uid, _ = await _make_user(cash=Decimal("50"), debt=Decimal("30"))
    async with async_session_maker() as s:
        u = await s.get(User, target_uid)
        from datetime import datetime, timezone
        u.debt_last_accrued_at = datetime.now(timezone.utc)
        await s.commit()
    _, h = await _make_user(superuser=True)
    r = await client.post(f"/api/v1/user/{target_uid}/forgive-debt",
                          json={"amount": "9999", "reason": "full wipe"}, headers=h)
    assert r.status_code == 200
    async with async_session_maker() as s:
        u = await s.get(User, target_uid)
    assert u.debt == Decimal("0.000000")
    assert u.debt_last_accrued_at is None
```

- [ ] **Step 2: 运行测试确认失败**

```bash
pytest tests/test_loan_admin.py -x -v
```
Expected: 404

- [ ] **Step 3: 实现**

在 `backend/app/api/v1/user.py` 末尾（紧接 `adjust-cash` 之后），追加两个 handler。先在顶部 import 区确认已有：
```python
from app.core.users import current_active_user, current_superuser
from app.services import site_config, loan_service
from app.schemas.loan import ForceLoanRequest, ForgiveDebtRequest
```
（如果 `current_superuser` 未从 `core/users` 导出，打开 `backend/app/core/users.py` 添加 `current_superuser = _require_superuser`。）

然后追加：
```python
@router.post("/{user_id}/force-loan", summary="管理员强制放贷")
async def force_loan(
    user_id: int,
    req: ForceLoanRequest,
    admin: User = Depends(current_superuser),
    db: AsyncSession = Depends(get_async_session),
):
    enabled = await site_config.get_bool(db, "loan_enabled")
    if not enabled:
        raise HTTPException(status_code=403, detail="借款功能已关闭")
    rate = await site_config.get_decimal(db, "loan_daily_rate")
    target = await db.get(User, user_id)
    if not target:
        raise HTTPException(status_code=404, detail="用户不存在")
    u = await loan_service.increase_debt(
        db, user_id, Decimal(req.amount), grant_cash=True, daily_rate=rate,
    )
    await db.commit()
    await db.refresh(u)
    logging.getLogger("thccb.admin").info(
        "FORCE_LOAN admin_id=%s user_id=%s amount=%s reason=%s new_cash=%s new_debt=%s",
        admin.id, user_id, req.amount, req.reason, u.cash, u.debt,
    )
    return {
        "user_id": user_id,
        "cash": float(u.cash.quantize(Decimal("0.01"))),
        "debt": float(u.debt.quantize(Decimal("0.01"))),
    }


@router.post("/{user_id}/forgive-debt", summary="管理员免除债务")
async def forgive_debt(
    user_id: int,
    req: ForgiveDebtRequest,
    admin: User = Depends(current_superuser),
    db: AsyncSession = Depends(get_async_session),
):
    rate = await site_config.get_decimal(db, "loan_daily_rate")
    target = await db.get(User, user_id)
    if not target:
        raise HTTPException(status_code=404, detail="用户不存在")
    u, effective = await loan_service.decrease_debt(
        db, user_id, Decimal(req.amount), consume_cash=False, daily_rate=rate,
    )
    await db.commit()
    await db.refresh(u)
    logging.getLogger("thccb.admin").info(
        "FORGIVE_DEBT admin_id=%s user_id=%s requested=%s effective=%s reason=%s new_debt=%s",
        admin.id, user_id, req.amount, effective, req.reason, u.debt,
    )
    return {
        "user_id": user_id,
        "cash": float(u.cash.quantize(Decimal("0.01"))),
        "debt": float(u.debt.quantize(Decimal("0.01"))),
        "effective": float(effective.quantize(Decimal("0.01"))),
    }
```

在 user.py 顶部 `import logging` 若未存在则加一行 `from decimal import Decimal`（已有则跳过）。

- [ ] **Step 4: 运行测试确认通过**

```bash
pytest tests/test_loan_admin.py -x -v
```
Expected: 5 passed

- [ ] **Step 5: Commit**

```bash
git add backend/app/api/v1/user.py backend/app/core/users.py backend/tests/test_loan_admin.py
git commit -m "feat(backend): 管理员 /user/{id}/force-loan + /forgive-debt + 5 单测"
```

---

## Task 15: market.buy/sell 集成 accrue

**Files:**
- Modify: `backend/app/api/v1/market.py`

- [ ] **Step 1: 定位锁行位置**

```bash
grep -n "with_for_update\|locked_user\|SELECT.*FOR UPDATE" backend/app/api/v1/market.py
```
记下 buy / sell 两处 `SELECT ... FOR UPDATE user` 之后紧接的那一行。

- [ ] **Step 2: 插入 accrue 调用**

在 buy 和 sell 两个函数里，**拿到 `locked_user` 后、改 `cash` / `debt` 前**，各加两行：
```python
from app.services.loan_service import accrue_interest as _accrue
from app.services import site_config as _cfg
# ...
_rate = await _cfg.get_decimal(db, "loan_daily_rate")
_accrue(locked_user, _rate, datetime.now(timezone.utc))
```

（import 挪到文件顶部，避免在函数里多次 import。`datetime` / `timezone` 应已 imported；如无则加。）

- [ ] **Step 3: 写交易兜底测试**

在 `backend/tests/test_loan_api.py` 追加：
```python
from datetime import datetime, timezone, timedelta


@pytest.mark.asyncio
async def test_buy_accrues_interest_before_charging(client):
    """有 debt 的用户调 buy 前，debt 应被 accrue 过。
    这个测试不做完整 buy 流程（依赖 market seed），只验证 accrue 在交易路径被调用。
    改由单测 check：直接调 loan_service.accrue 然后 assert 改动。"""
    uid, _ = await _make_user(cash=Decimal("1000"), debt=Decimal("100"))
    async with async_session_maker() as s:
        u = await s.get(User, uid)
        u.debt_last_accrued_at = datetime.now(timezone.utc) - timedelta(days=1)
        await s.commit()
    # 简化：直接调 accrue 验证函数可用（真正的集成测试等 Task 17 手测覆盖）
    from app.services.loan_service import accrue_interest
    async with async_session_maker() as s:
        u = await s.get(User, uid)
        before = u.debt
        accrue_interest(u, Decimal("0.01"), datetime.now(timezone.utc))
        assert u.debt > before
```

> 备注：`buy` / `sell` 完整端到端测试需要 market seed（Market + Outcome + positions），成本较高；交易兜底的正确性由 loan_service.accrue_interest 的单测 + 手测覆盖即可。

- [ ] **Step 4: 回归跑所有 loan 相关测试**

```bash
pytest tests/test_loan_service.py tests/test_loan_sweep.py tests/test_loan_api.py tests/test_loan_admin.py tests/test_site_config.py tests/test_site_config_api.py -x -v
```
Expected: 全部 passed

再跑一遍主 import 自检：
```bash
python -c "import app.main; print('ok')"
```

- [ ] **Step 5: Commit**

```bash
git add backend/app/api/v1/market.py backend/tests/test_loan_api.py
git commit -m "feat(backend): market.buy/sell 交易兜底调 accrue_interest"
```

---

## Task 16: 后端整体自检

**Files:** 无

- [ ] **Step 1: 语法扫描**

```bash
cd backend && python -m py_compile $(find app -name '*.py')
```
Expected: 无输出

- [ ] **Step 2: import 自检**

```bash
python -c "import app.main; print('ok')"
```
Expected: `ok`

- [ ] **Step 3: 全量 pytest**

```bash
pytest -x
```
Expected: 全部 passed

- [ ] **Step 4: 写 ralph-log 后端阶段总结**

追加到 `docs/ralph-log.md`：
```markdown
## 2026-04-24 <HH:MM> — LoanV1 后端完成
**目标**：加贷款功能（借/还/复利/管理员配置与强制放贷/免债）
**范围**：backend/app/services/{site_config,loan_service,loan_sweep}.py；backend/app/api/v1/{loan,site_config}.py；user.py 加管理员端点；market.py 交易兜底；模型加 debt_last_accrued_at 和 SiteConfig 表
**改动**：见 git log ralph/2026-04-24-loan-impl
**风险 & 回滚**：迁移脚本未运行，待合并前人工执行；revert 分支即可完全回滚
**验证**：py_compile ✅ / pytest ✅ / 端到端手测留到前端完成后
**下一轮**：前端 loan 页 + 管理员配置页 + Header 徽章
```

- [ ] **Step 5: Commit**

```bash
git add docs/ralph-log.md
git commit -m "docs(ralph): LoanV1 后端阶段总结"
```

---

## Task 17: 前端 API client 和 store

**Files:**
- Create: `thccb-frontend/src/api/loan.ts`
- Create: `thccb-frontend/src/stores/loan.ts`
- Modify: `thccb-frontend/src/api/index.ts`

- [ ] **Step 1: 读现有 API 风格**

```bash
cat thccb-frontend/src/api/user.ts thccb-frontend/src/api/index.ts
```
记下 axios 实例名、拦截器、类型命名约定。

- [ ] **Step 2: 写 loan API**

`thccb-frontend/src/api/loan.ts`：
```ts
import { request } from './index'

export interface LoanQuota {
  enabled: boolean
  cash: string
  debt: string
  net_worth: string
  leverage_k: string
  daily_rate: string
  max_borrow: string
  last_accrued_at: string | null
}

export interface LoanActionResult {
  cash: string
  debt: string
  max_borrow: string
}

export const loanApi = {
  quota: () => request.get<LoanQuota>('/loan/quota').then(r => r.data),
  borrow: (amount: string) => request.post<LoanActionResult>('/loan/borrow', { amount }).then(r => r.data),
  repay: (amount: string) => request.post<LoanActionResult>('/loan/repay', { amount }).then(r => r.data),
}

export interface SiteConfigItem {
  key: string
  value: string
  value_type: string
  updated_at: string
  updated_by: number | null
}

export const adminSiteConfigApi = {
  list: () => request.get<SiteConfigItem[]>('/admin/site-config').then(r => r.data),
  update: (key: string, value: string) =>
    request.put<SiteConfigItem>(`/admin/site-config/${key}`, { value }).then(r => r.data),
  forceLoan: (userId: number, amount: string, reason: string) =>
    request.post(`/user/${userId}/force-loan`, { amount, reason }).then(r => r.data),
  forgiveDebt: (userId: number, amount: string, reason: string) =>
    request.post(`/user/${userId}/forgive-debt`, { amount, reason }).then(r => r.data),
}
```

> `request` 实例名若不一样（可能是 `api` / `http`），照 `user.ts` 里的改。

- [ ] **Step 3: 在 api/index.ts 里导出**

在 `thccb-frontend/src/api/index.ts` 末尾追加：
```ts
export * from './loan'
```

- [ ] **Step 4: 写 Pinia store**

`thccb-frontend/src/stores/loan.ts`：
```ts
import { defineStore } from 'pinia'
import { ref } from 'vue'
import { loanApi, type LoanQuota } from '@/api/loan'

export const useLoanStore = defineStore('loan', () => {
  const quota = ref<LoanQuota | null>(null)
  const loading = ref(false)
  const error = ref<string | null>(null)

  async function refresh() {
    loading.value = true
    error.value = null
    try {
      quota.value = await loanApi.quota()
    } catch (e: any) {
      error.value = e?.message ?? '加载失败'
    } finally {
      loading.value = false
    }
  }

  async function borrow(amount: string) {
    const r = await loanApi.borrow(amount)
    if (quota.value) {
      quota.value.cash = r.cash
      quota.value.debt = r.debt
      quota.value.max_borrow = r.max_borrow
    }
    return r
  }

  async function repay(amount: string) {
    const r = await loanApi.repay(amount)
    if (quota.value) {
      quota.value.cash = r.cash
      quota.value.debt = r.debt
      quota.value.max_borrow = r.max_borrow
    }
    return r
  }

  return { quota, loading, error, refresh, borrow, repay }
})
```

- [ ] **Step 5: 类型检查**

```bash
cd /data/sunyunbo/www/TouhouCCB && npm run type-check
```
Expected: 无新增 error

- [ ] **Step 6: Commit**

```bash
git add thccb-frontend/src/api/loan.ts thccb-frontend/src/api/index.ts thccb-frontend/src/stores/loan.ts
git commit -m "feat(frontend): loan API client + Pinia store"
```

---

## Task 18: 前端 Loan 页面

**Files:**
- Create: `thccb-frontend/src/pages/loan/Loan.vue`
- Modify: `thccb-frontend/src/router/routes.ts`

- [ ] **Step 1: 读风格样板**

```bash
cat thccb-frontend/src/pages/user/Portfolio.vue | head -80
```
记下：容器类名、颜色 token、NInput / NButton 使用、黑白工业风样式。

- [ ] **Step 2: 写页面**

`thccb-frontend/src/pages/loan/Loan.vue`：
```vue
<script setup lang="ts">
import { onMounted, ref, computed } from 'vue'
import { NInput, NButton, NSpin, NAlert, NDivider, useMessage } from 'naive-ui'
import { useLoanStore } from '@/stores/loan'

const store = useLoanStore()
const msg = useMessage()

const borrowAmount = ref('')
const repayAmount = ref('')
const submitting = ref(false)

onMounted(() => store.refresh())

const dailyRatePct = computed(() => {
  const r = store.quota?.daily_rate
  if (!r) return '—'
  return (Number(r) * 100).toFixed(2) + '%'
})

async function submitBorrow() {
  if (!borrowAmount.value) return
  submitting.value = true
  try {
    await store.borrow(borrowAmount.value)
    msg.success(`借入 ${borrowAmount.value}`)
    borrowAmount.value = ''
  } catch (e: any) {
    msg.error(e?.response?.data?.detail ?? '借款失败')
  } finally { submitting.value = false }
}

async function submitRepay() {
  if (!repayAmount.value) return
  submitting.value = true
  try {
    await store.repay(repayAmount.value)
    msg.success(`还款 ${repayAmount.value}`)
    repayAmount.value = ''
  } catch (e: any) {
    msg.error(e?.response?.data?.detail ?? '还款失败')
  } finally { submitting.value = false }
}

function repayAll() {
  if (!store.quota) return
  repayAmount.value = store.quota.debt
  submitRepay()
}
</script>

<template>
  <div class="loan-page">
    <NSpin :show="store.loading">
      <NAlert v-if="store.error" type="error" :title="store.error" />
      <NAlert v-else-if="store.quota && !store.quota.enabled" type="warning" title="借款功能已关闭" />

      <section class="panel">
        <h2>负债</h2>
        <div class="debt-number" :class="{ red: Number(store.quota?.debt) > 0 }">
          {{ store.quota?.debt ?? '—' }}
        </div>
        <div class="meta">
          可借额度：<strong>{{ store.quota?.max_borrow ?? '—' }}</strong>　·　
          日利率：<strong>{{ dailyRatePct }}</strong>　·　
          现金：<strong>{{ store.quota?.cash ?? '—' }}</strong>
        </div>
        <div class="meta-small" v-if="store.quota?.last_accrued_at">
          上次结息：{{ new Date(store.quota.last_accrued_at).toLocaleString() }}
        </div>
      </section>

      <NDivider />

      <section class="panel">
        <h3>借款</h3>
        <div class="row">
          <NInput v-model:value="borrowAmount" placeholder="金额"
                  :disabled="!store.quota?.enabled" />
          <NButton type="primary" :loading="submitting" :disabled="!store.quota?.enabled"
                   @click="submitBorrow">借入</NButton>
        </div>
      </section>

      <section class="panel">
        <h3>还款</h3>
        <div class="row">
          <NInput v-model:value="repayAmount" placeholder="金额" />
          <NButton :loading="submitting" @click="submitRepay">还款</NButton>
          <NButton quaternary @click="repayAll" :disabled="!store.quota || Number(store.quota.debt) <= 0">
            全部还清
          </NButton>
        </div>
      </section>
    </NSpin>
  </div>
</template>

<style scoped>
.loan-page { padding: 16px; max-width: 640px; }
.panel { margin-bottom: 16px; border: 2px solid #000; padding: 16px; background: #fff; }
.debt-number { font-size: 40px; font-weight: 700; }
.debt-number.red { color: #d14; }
.meta { margin-top: 8px; color: #555; }
.meta-small { margin-top: 4px; font-size: 12px; color: #888; }
.row { display: flex; gap: 8px; align-items: center; }
h2, h3 { margin: 0 0 8px 0; }
</style>
```

- [ ] **Step 3: 加路由**

在 `thccb-frontend/src/router/routes.ts` 里，找到 `user/transactions` 路由那一段，其后追加：
```ts
      {
        path: 'loan',
        name: 'loan',
        component: () => import('@/pages/loan/Loan.vue'),
        meta: {
          title: '借款',
          requiresAuth: true,
          requiresVerified: true,
        },
      },
```

- [ ] **Step 4: 类型检查 + lint**

```bash
cd /data/sunyunbo/www/TouhouCCB && npm run type-check && npm run lint
```
Expected: 无新增 error

- [ ] **Step 5: Commit**

```bash
git add thccb-frontend/src/pages/loan/Loan.vue thccb-frontend/src/router/routes.ts
git commit -m "feat(frontend): 新增 /loan 页 — 借款/还款/额度展示"
```

---

## Task 19: Header 负债徽章

**Files:**
- Modify: `thccb-frontend/src/layouts/DefaultLayout.vue`

- [ ] **Step 1: 找到现金显示位置**

```bash
grep -n "cash\|现金\|userStore" thccb-frontend/src/layouts/DefaultLayout.vue
```

- [ ] **Step 2: 加徽章**

在现金显示的 template 块旁边（同一 `<div class="wallet">` 或同级），追加：
```vue
<RouterLink to="/loan"
  v-if="Number(userStore.user?.debt ?? 0) > 0"
  class="debt-badge">
  负债 {{ userStore.user?.debt }}
</RouterLink>
```

script 块若未 import RouterLink 则：
```ts
import { RouterLink } from 'vue-router'
```

style 块追加：
```css
.debt-badge {
  display: inline-block;
  padding: 2px 8px;
  margin-left: 8px;
  border: 2px solid #d14;
  color: #d14;
  font-weight: 700;
  text-decoration: none;
  font-size: 12px;
}
.debt-badge:hover { background: #d14; color: #fff; }
```

> 若 `userStore.user` 结构没有 `debt`，先确认 `/user/me` 返回含 `debt`（spec §4.1 应已含，`user.py:98` 处返回 `cash`，需同步加 `debt`）。如未含，在 `user.py` 的 `/me` 响应里加一行 `"debt": user.debt.quantize(Decimal("0.01"))`，并更新前端类型。

- [ ] **Step 3: type-check + lint**

```bash
npm run type-check && npm run lint
```

- [ ] **Step 4: Commit**

```bash
git add thccb-frontend/src/layouts/DefaultLayout.vue thccb-frontend/src/stores/user.ts thccb-frontend/src/api/user.ts backend/app/api/v1/user.py
git commit -m "feat(frontend): Header 负债徽章（debt>0 时红色边框链 /loan）"
```

---

## Task 20: 管理员 SiteConfig 页

**Files:**
- Create: `thccb-frontend/src/pages/admin/SiteConfig.vue`
- Modify: `thccb-frontend/src/router/routes.ts`

- [ ] **Step 1: 读现有 admin 页样式**

```bash
cat thccb-frontend/src/pages/admin/MarketManage.vue | head -60
```

- [ ] **Step 2: 写 SiteConfig 页**

`thccb-frontend/src/pages/admin/SiteConfig.vue`：
```vue
<script setup lang="ts">
import { onMounted, ref } from 'vue'
import { NTable, NInput, NButton, NSpin, NAlert, useMessage, NInputNumber, NDivider } from 'naive-ui'
import { adminSiteConfigApi, type SiteConfigItem } from '@/api/loan'

const configs = ref<SiteConfigItem[]>([])
const loading = ref(false)
const error = ref<string | null>(null)
const drafts = ref<Record<string, string>>({})
const msg = useMessage()

// 强制放贷 / 免债
const targetUserId = ref<number | null>(null)
const forceAmount = ref<string>('')
const forceReason = ref<string>('')
const forgiveAmount = ref<string>('')
const forgiveReason = ref<string>('')

async function load() {
  loading.value = true
  try {
    configs.value = await adminSiteConfigApi.list()
    drafts.value = Object.fromEntries(configs.value.map(c => [c.key, c.value]))
  } catch (e: any) { error.value = e?.message ?? '加载失败' }
  finally { loading.value = false }
}

async function save(key: string) {
  try {
    await adminSiteConfigApi.update(key, drafts.value[key])
    msg.success(`${key} 已更新`)
    await load()
  } catch (e: any) { msg.error(e?.response?.data?.detail ?? '更新失败') }
}

async function doForceLoan() {
  if (!targetUserId.value) return msg.error('输入用户 ID')
  try {
    await adminSiteConfigApi.forceLoan(targetUserId.value, forceAmount.value, forceReason.value)
    msg.success('放贷成功')
    forceAmount.value = ''; forceReason.value = ''
  } catch (e: any) { msg.error(e?.response?.data?.detail ?? '失败') }
}

async function doForgiveDebt() {
  if (!targetUserId.value) return msg.error('输入用户 ID')
  try {
    await adminSiteConfigApi.forgiveDebt(targetUserId.value, forgiveAmount.value, forgiveReason.value)
    msg.success('免债成功')
    forgiveAmount.value = ''; forgiveReason.value = ''
  } catch (e: any) { msg.error(e?.response?.data?.detail ?? '失败') }
}

onMounted(load)
</script>

<template>
  <div class="admin-site-config">
    <NSpin :show="loading">
      <NAlert v-if="error" type="error" :title="error" />

      <h2>站点配置</h2>
      <NTable :bordered="true" :single-line="false">
        <thead>
          <tr><th>Key</th><th>类型</th><th>当前值</th><th>更新时间</th><th>操作</th></tr>
        </thead>
        <tbody>
          <tr v-for="c in configs" :key="c.key">
            <td>{{ c.key }}</td>
            <td>{{ c.value_type }}</td>
            <td><NInput v-model:value="drafts[c.key]" size="small" /></td>
            <td>{{ new Date(c.updated_at).toLocaleString() }}</td>
            <td><NButton size="small" type="primary" @click="save(c.key)">保存</NButton></td>
          </tr>
        </tbody>
      </NTable>

      <NDivider />

      <h2>强制放贷 / 免债</h2>
      <div class="row">
        <span>目标用户 ID：</span>
        <NInputNumber v-model:value="targetUserId" placeholder="user_id" />
      </div>

      <h3>强制放贷（受 loan_enabled 约束）</h3>
      <div class="row">
        <NInput v-model:value="forceAmount" placeholder="金额" />
        <NInput v-model:value="forceReason" placeholder="原因" />
        <NButton type="warning" @click="doForceLoan">放贷</NButton>
      </div>

      <h3>免除债务</h3>
      <div class="row">
        <NInput v-model:value="forgiveAmount" placeholder="金额" />
        <NInput v-model:value="forgiveReason" placeholder="原因" />
        <NButton @click="doForgiveDebt">免债</NButton>
      </div>
    </NSpin>
  </div>
</template>

<style scoped>
.admin-site-config { padding: 16px; }
.row { display: flex; gap: 8px; align-items: center; margin: 8px 0; }
h2 { margin-top: 16px; }
</style>
```

- [ ] **Step 3: 加路由**

在 `routes.ts` 里找到 `admin/MarketManage` 路由（若有），同级追加：
```ts
      {
        path: 'admin/site-config',
        name: 'admin-site-config',
        component: () => import('@/pages/admin/SiteConfig.vue'),
        meta: {
          title: '站点配置',
          requiresAuth: true,
          requiresVerified: true,
          requiresSuperuser: true,  // 若现有 guard 支持该 meta
        },
      },
```
（现有 guards 不支持 `requiresSuperuser` 也不影响——后端鉴权仍拦。）

- [ ] **Step 4: type-check + lint**

```bash
npm run type-check && npm run lint
```

- [ ] **Step 5: Commit**

```bash
git add thccb-frontend/src/pages/admin/SiteConfig.vue thccb-frontend/src/router/routes.ts
git commit -m "feat(frontend): 管理员站点配置 + 强制放贷 + 免债 UI"
```

---

## Task 21: 浏览器端到端手测 + 部署文档

**Files:**
- Modify: `docs/deploy.md`
- Modify: `docs/ralph-log.md`

- [ ] **Step 1: 启动前端 dev（或 build）**

```bash
cd /data/sunyunbo/www/TouhouCCB && npm run dev
```
或：
```bash
npm run build
```
（后端已经在本地/容器跑着；否则先起后端容器，跑迁移脚本 `python -m scripts.migrate_loan_v1`。）

- [ ] **Step 2: 手测主路径**

打开浏览器登录：
1. 访问 `/loan`：看到 debt=0、cash、max_borrow 正确
2. 借 100：刷新，debt 和 cash 各 +100；Header 出现红色"负债 100"徽章
3. 等 1-2 分钟后刷新：debt 稍有增长（复利生效）
4. 还 50：debt 和 cash 各 -50
5. 输"9999"全额还：debt 归 0，cash 减到对应额度，徽章消失
6. 超借（amount > max_borrow）：toast 报错
7. 超还（amount > cash）：toast 报错
8. 管理员登录 → `/admin/site-config`：
   - 改 `loan_enabled=false`，再以普通账号试借款 → 403
   - 改回 `true`，改 `loan_daily_rate=0.05`
   - 强制给某用户放贷 500 → 对方 cash/debt 各 +500
   - 免除某用户 100 → 对方 debt 只减 100，cash 不变
9. 移动端尺寸：页面布局不错位

记录手测结果。如有 UI 崩坏，新分支修。

- [ ] **Step 3: 更新 docs/deploy.md**

在合适位置追加：
````markdown
## LoanV1 上线步骤（2026-04-24）

**1. 停服或低流量窗口运行迁移脚本（幂等，可重跑）：**

```bash
cd backend && source venv/bin/activate && python -m scripts.migrate_loan_v1
```

**2. 重启后端容器以注册 APS sweep：**

```bash
docker compose restart backend
```

**3. 校验：**
- `GET /api/v1/admin/site-config` 返回 4 条 `loan_*` 配置
- 服务日志应出现 `loan sweep started interval=60s`
- 随便登录一个账号访问 `/loan` 页，功能可用

**回滚：**
- revert merge commit 即可；迁移加的列/表保留无害（`debt_last_accrued_at` 全为 NULL，SiteConfig 表闲置）。
````

- [ ] **Step 4: 写 ralph-log 最终总结**

```markdown
## 2026-04-24 <HH:MM> — LoanV1 完成（前后端 + 部署）
**目标**：贷款系统 V1 全量上线（见 docs/superpowers/specs/2026-04-24-loan-design.md）
**范围**：backend + thccb-frontend + docs/deploy.md
**改动**：见 ralph/2026-04-24-loan-impl 完整 commit 列表
**风险 & 回滚**：迁移脚本待运行；revert 分支回滚无数据损伤
**验证**：后端 pytest ✅ / 前端 type-check ✅ / lint ✅ / 浏览器端到端手测主路径+边界 ✅
**下一轮**：观察生产利息累积精度 1-2 周后，再决定是否进 LoanV2（保证金 + 强平）
```

- [ ] **Step 5: Commit**

```bash
git add docs/deploy.md docs/ralph-log.md
git commit -m "docs: LoanV1 部署步骤 + ralph-log 最终总结"
```

---

## 合入说明（结束时读一遍）

- **不要 push**（push = 自动部署，需人工确认）
- **合 main 前必须**：人工在 staging 跑迁移脚本验证幂等；跑一次端到端手测
- **合并方式**：按 CLAUDE.md 习惯，在 main 做 `merge --no-ff` commit（消息如 `merge ralph/2026-04-24-loan-impl: LoanV1 (21 commits)`）
- **部署顺序**：先合代码（不重启）→ 跑迁移 → 重启 backend

---

## Self-Review 要点（我 writer 自己过的）

- **Spec §2 数据模型**：Task 2（列 + 表）+ Task 3（迁移）
- **Spec §3 sweep + accrue**：Task 5（accrue 单元）+ Task 7（sweep）+ Task 8（lifespan）
- **Spec §3.5 服务层**：Task 5 + 6 原子操作
- **Spec §4 API**：Task 10/11/12（玩家）+ Task 13（site-config）+ Task 14（force-loan/forgive）
- **Spec §5 前端**：Task 17（store/api）+ 18（loan 页）+ 19（Header 徽章）+ 20（admin 页）
- **Spec §6 测试**：每任务含 TDD 步骤；市场 buy/sell 端到端测试标注走手测覆盖
- **Spec §7 红线**：Task 3 commit 里明确脚本不运行；合入说明重复强调
- **Spec §8 回滚**：Task 21 deploy.md 和 ralph-log 写明

**已知简化**：
- `buy`/`sell` 完整集成测试不写（需要大量 market seed），改为手测覆盖（Task 21 step 2）
- 每日利息汇总 log 未实现（spec §3.2 可选项；若需要可单独加一个 cron）
- 前端无 e2e 自动化测试，沿用 CLAUDE.md 的手测约定
