# ========================================
# Docker Compose 变量（docker-compose.yml 读取此文件）
# 复制为 .env 并填入实际值
# ========================================

# PostgreSQL 密码（必须与 backend/.env 中的 PG_PASSWORD 一致）
PG_PASSWORD=change_me

# 容器内用户 UID:GID（与宿主机部署用户一致，用 id -u && id -g 查看）
UID=1007
GID=1008
